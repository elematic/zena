// @mode: run
// @result: 15

class A {
  x: i32 = 5;  
}

class B extends A {
  y: i32 = 10;
}

export let main = (): i32 => {
  let a: A = new B();
  let b = a as B; // Downcast back to subclass
  return b.x + b.y;
};
