// @mode: run
// @result: 5

class Foo {
  x: i32 = 5;
}

let makeFoo = (make: boolean): Foo | null => {
  return if (make) { new Foo() } else { null };
};

export let main = (): i32 => {
  let foo = makeFoo(true);
  return (foo as Foo).x;
};
