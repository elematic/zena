// @mode: run
// @result: 100

var sideEffect = 0;

class Box {
  value: i32 = 0;
  new(v: i32) : value = v;
}

let makeBox = () => {
  sideEffect = 100;
  return new Box(77);
};

export let main = () => {
  let b: Box | null = null;
  let result = b ?? makeBox();
  return sideEffect;
};
