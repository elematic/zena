// @mode: run
// @result: 99

class Box {
  value: i32 = 0;
  new(v: i32) { this.value = v; }
}

export let main = () => {
  let b: Box | null = null;
  let result = b ?? new Box(99);
  return result.value;
};
