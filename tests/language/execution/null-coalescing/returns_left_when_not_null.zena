// @mode: run
// @result: 42

class Box {
  value: i32 = 0;
  new(v: i32) : value = v;
}

export let main = () => {
  let b: Box | null = new Box(42);
  let result = b ?? new Box(0);
  return result.value;
};
