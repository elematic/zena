// @mode: run
// @result: 300

class Point {
  var(#x) x: i32;
  var(#y) y: i32;

  new(x: i32, y: i32) : #x = x, #y = y;

  increment() {
    this.#x = this.#x + 1;
    this.#y = this.#y + 1;
  }
}

export let main = (): i32 => {
  let p = new Point(9, 29);
  p.increment();
  return p.x * p.y;
};
