// @mode: run
// @result: 26

class Base {
  x: i32;
  new(this.x);
}

class Derived extends Base {

  new(x: i32) : super(x);

  x: i32 {
    get {
      return super.x * 2;
    }
  }
}

export let main = (): i32 => {
  let d = new Derived(13);
  return d.x;
};
