// @mode: run
// @result: 320

class Point {
  #x: i32;
  #y: i32;

  new(x: i32, y: i32) : #x = x, #y = y;

  getArea(): i32 {
    return this.#x * this.#y;
  }
}

export let main = (): i32 => {
  let p = new Point(10, 32);
  return p.getArea();
};
