// @mode: run
// @result: 50

class Base {
  doMath(n: i32): i32 {
    return n * 2;
  }
}

class Derived extends Base {
  doMath(n: i32): i32 {
    return n * 5;
  }
}

export let main = (): i32 => {
  let b: Base = new Derived();
  return b.doMath(10);
};
