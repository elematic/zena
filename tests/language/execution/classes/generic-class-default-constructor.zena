// @mode: run
// @result: 42

class Box<T> {
  value: T;
  new(this.value);
}

class Wrapper<T> {
  box: Box<T> = new Box<T>(42 as T);
}

export let main = (): i32 => {
  let w = new Wrapper<i32>();
  return w.box.value;
};
