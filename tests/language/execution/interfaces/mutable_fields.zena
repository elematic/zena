// @mode: run
// @invoke: main
// @result: 55

interface Counter {
  var count: i32;
}

class SimpleCounter implements Counter {
  var count: i32;
  new(this.count);
}

export let main = (): i32 => {
  let c: Counter = new SimpleCounter(10);
  c.count = 55;
  return c.count;
};
