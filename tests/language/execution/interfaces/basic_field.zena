// @mode: run
// @invoke: main
// @result: 50

interface Point {
  x: i32;
  y: i32;
}

class Point2D implements Point {
  x: i32;
  y: i32;
  new(this.x, this.y);
}

export let main = (): i32 => {
  let p: Point = new Point2D(15, 35);
  return p.x + p.y;
};
