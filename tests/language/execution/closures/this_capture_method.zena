// @mode: run
// @result: 2

class Counter {
  var value: i32 = 0;

  increment() {
    this.value = this.value + 1;
  }

  getValue(): i32 {
    return this.value;
  }
}

export let main = () => {
  let c = new Counter();
  c.increment();
  c.increment();
  return c.getValue();
};
