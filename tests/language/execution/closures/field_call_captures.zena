// @mode: run
// @result: 42

class Incrementer {
  fn: () => i32;
  new(fn: () => i32) : fn = fn {}
  call(): i32 {
    return this.fn();
  }
}

export let main = (): i32 => {
  let base = 40;
  let getVal = (): i32 => base + 2;
  let inc = new Incrementer(getVal);
  return inc.call();
};
