// @mode: run
// @result: 3

class Counter {
  var count: i32 = 0;
  increment() { this.count += 1; }
}

export let main = () => {
  var c = new Counter();
  let inc = () => { c.increment(); };
  let read = () => c.count;
  inc();
  inc();
  inc();
  return read();
};