// @mode: run
// @result: 42

class Box<T> {
  item: T;
  new(item: T) : item = item {}
  makeGetter(): () => T {
    return () => this.item;
  }
}

export let main = () => {
  let box = new Box<i32>(42);
  let getter = box.makeGetter();
  return getter();
};
