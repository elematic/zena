// @mode: run
// @result: 42

class Handler {
  fn: (x: i32) => i32;
  new(fn: (x: i32) => i32) : fn = fn {}
  invoke(x: i32): i32 {
    return this.fn(x);
  }
}

export let main = (): i32 => {
  let double = (x: i32): i32 => x * 2;
  let h = new Handler(double);
  return h.invoke(21);
};
