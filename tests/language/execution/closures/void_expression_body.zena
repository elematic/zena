// @mode: run
// @result: 11
// A void expression-bodied closure whose body is itself a void call.

var sink = 0;
let bump = (n: i32): void => {
  sink += n;
};

export let main = (): i32 => {
  let voidly = (n2: i32): void => bump(n2);
  voidly(5);
  voidly(6);
  return sink;
};
