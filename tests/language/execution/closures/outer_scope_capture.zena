// @mode: run
// @result: 5

export let main = () => {
  var x = 1;
  let makeAdder = () => {
    return () => {
      x = x + 2;
    };
  };
  let adder = makeAdder();
  adder();
  adder();
  return x;
};