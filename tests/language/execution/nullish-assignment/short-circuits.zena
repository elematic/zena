// @mode: run
// @result: 0

class Box {
  value: i32 = 0;
  new(value: i32) : value = value;
}

var sideEffect: i32 = 0;

let makeBox = (): Box => {
  sideEffect = 1;
  return new Box(99);
};

export let main = () => {
  var b: Box | null = new Box(10);
  b ??= makeBox();
  return sideEffect;
};
