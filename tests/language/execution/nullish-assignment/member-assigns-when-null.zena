// @mode: run
// @result: 42

class Box {
  value: i32 = 0;
  new(value: i32) : value = value;
}

class Holder {
  var box: Box | null = null;
}

export let main = () => {
  let h = new Holder();
  let b = h.box ??= new Box(42);
  return b.value + (if (h.box != null) 0 else 100);
};
