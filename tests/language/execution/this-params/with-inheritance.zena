// @mode: run
// @result: 30
class Base {
  var x: i32;
  new(x: i32) : x = x {}
}

class Derived extends Base {
  y: i32;
  new(this.y, x: i32) : super(x) {}
}

export let main = () => {
  let d = new Derived(20, 10);
  return d.x + d.y;
};
