// @mode: run
// @result: 0
// Self-recursive function with return type annotation.
let countdown = (n: i32): void => {
  if (n > 0) {
    countdown(n - 1);
  }
};

export let main = () => {
  countdown(10);
  return 0;
};
