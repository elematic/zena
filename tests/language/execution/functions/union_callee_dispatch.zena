// @skip: bootstrap
// @mode: run
// @result: 42

class Base {}

export let main = (): i32 => {
  let f1 = (x: Base) => 42;
  let f2 = (x: Base) => 100;
  
  let cond = true;
  let f = if (cond) f1 else f2;
  
  return f(new Base());
};
