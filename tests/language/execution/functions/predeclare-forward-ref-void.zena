// @mode: run
// @result: 0
// Mutual recursion between void functions with return type annotations.
let process = (n: i32): void => {
  helper(n);
};

let helper = (n: i32): void => {
  if (n > 0) {
    process(n - 1);
  }
};

export let main = () => {
  process(5);
  return 0;
};
