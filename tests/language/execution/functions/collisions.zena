// @mode: run
// @result: true

// Generate a collision with the i32ToString function we use for printing numbers
export let i32ToString = (): i32 => {
  return 20;
};

export let main = (): boolean => {
  return `a${i32ToString()}b` == `a20b`;
};
