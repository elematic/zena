// @mode: run
// @result: 42

let identity = <T>(x: T): T => {
  return x;
};

class Holder<T> {
  val: T;
  new(this.val);
  
  getVal(): T {
    let id: (x: T) => T = identity;
    return id(this.val);
  }
}

export let main = (): i32 => {
  let h = new Holder<i32>(42);
  return h.getVal();
};
