// @mode: run
// @result: 17

let add = (a: i32, b: i32 = 7): i32 => {
  return a + b;
};

export let main = (): i32 => {  
  let r1 = add(5);
  let r2 = add(2, 3);
  return r1 + r2; // Should be (5 + 7) + (2 + 3) = 17
};
