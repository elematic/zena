// @mode: run
// @result: 42

class Bar {
  x: i32;
  new(x: i32) : x = x {}
}

class Foo {
  var bar?: Bar;
  new() {}
}

export let main = (): i32 => {
  let foo = new Foo();
  foo.bar = new Bar(42);
  if (foo.bar != null) {
    let b = foo.bar as Bar;
    return b.x;
  }
  return 0;
};
