// @mode: run
// @result: 42
// Object-pattern destructuring with `as` renames in declarations.

class Point {
  x: i32;
  y: i32;
  new(this.x, this.y);
}

export let main = (): i32 => {
  let { x as first, y as second } = new Point(30, 12);
  return first + second;
};
