// @mode: run
// @result: 42
// Destructuring a boxed tuple held in a variable (not a call result).

export let main = (): i32 => {
  let t: (i32, i32) = (30, 12);
  let (a, b) = t;
  return a + b;
};
