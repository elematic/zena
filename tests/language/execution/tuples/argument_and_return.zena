// @mode: run
// @result: 42
// Boxed tuples as function arguments and pass-through returns.

let swap = (t: (i32, i32)): (i32, i32) => {
  return (t[1], t[0]);
};

export let main = (): i32 => {
  let s = swap((12, 30));
  return s[0] + s[1];
};
