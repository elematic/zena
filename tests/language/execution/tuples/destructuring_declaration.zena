// @mode: run
// @result: 42
// Tuple destructuring declarations over a multi-value call result.

let pair = (): (i32, i32) => {
  return (30, 12);
};

export let main = (): i32 => {
  let (a, b) = pair();
  return a + b;
};
