// @mode: run
// @result: 42
// Mixed unit and inline variants
sealed class Token {
  case Plus, Minus
  case Number(value: i32)
}

export let main = (): i32 => {
  let t: Token = new Number(42);
  return match (t) {
    case Plus: 0
    case Minus: 0
    case Number { value as v }: v
  };
};
