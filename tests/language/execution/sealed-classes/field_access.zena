// @mode: run
// @result: 10
// Sealed class field access
sealed class Shape {
  case Circle(radius: i32)
  case Rect(width: i32, height: i32)
}

export let main = (): i32 => {
  let c = new Circle(10);
  return c.radius;
};
