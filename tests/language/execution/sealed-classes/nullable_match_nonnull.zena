// @mode: run
// @result: 42
// Match on class | null with class pattern (non-null case)
class Box { value: i32; new(this.value); }

let unwrap = (x: Box | null): i32 => match (x) {
  case Box {value}: value
  case _: -1
};

export let main = (): i32 => unwrap(new Box(42));
