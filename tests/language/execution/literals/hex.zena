// @mode: run
// @result: 271
// Hex integer literals parse at i32 width, in declarations and in
// expression position.

export let main = (): i32 => {
  let h = 0xFF;
  if (h == 255 && 0x10 == 16) {
    return 0x10F;
  }
  return 0;
};
