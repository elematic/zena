// @mode: run
// @result: 42

symbol mySym;

class Foo {
  var [mySym]: i32;
  new(val: i32) {
    this.[mySym] = val;
  }
}

export let main = (): i32 => {
  let f = new Foo(42);
  return f.[mySym];
};
