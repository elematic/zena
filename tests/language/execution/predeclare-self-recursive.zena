// @mode: run
// @result: 0
// Self-recursive function with no return type annotation.
let countdown = (n: i32) => {
  if (n > 0) {
    countdown(n - 1);
  }
};

export let main = () => {
  countdown(10);
  return 0;
};
