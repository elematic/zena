// A helper module whose exports are `function` declarations rather than
// arrow-bound `let`s.
export function addOne(x: i32): i32 {
  return x + 1;
}

export function tripleIt(x: i32): i32 {
  return x * 3;
}
