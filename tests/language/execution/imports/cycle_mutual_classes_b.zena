import { Branch } from './cycle_mutual_classes.zena';

export class Leaf {
  var back: Branch | null = null;

  hook(b: Branch): void {
    this.back = b;
  }

  score(): i32 {
    let b = this.back;
    if (b == null) {
      return 0;
    }
    return (b as Branch).weight * 2;
  }
}
