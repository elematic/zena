export distinct type StepId = i32;

export final class Gen<T> {
  var #nextId: i32;
  new(nextId: i32 = 1) : #nextId = nextId;
  next(): T {
    let id = this.#nextId;
    this.#nextId += 1;
    return id as T;
  }
}
