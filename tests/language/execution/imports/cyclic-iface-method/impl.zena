import { Seq } from './counter.zena';
export final class Cell implements Seq { first(): i32 { return 42; } }
function pick<S extends Seq>(s: S): i32 { return s.first(); }
export let compute = (): i32 => pick(new Cell());
