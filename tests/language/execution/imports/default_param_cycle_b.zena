import {Ping} from './default_param_cycle.zena';

export class Echo {
  value: i32;
  new(this.value);

  get(): i32 {
    return this.value;
  }

  wrap(): Ping {
    return new Ping(this.value);
  }
}
