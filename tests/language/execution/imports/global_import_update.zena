// @mode: run
// @invoke: main
// @result: 82

import {remoteGlobal, remoteUpdate} from './global_helper.zena';

export let main = (): i32 => {
  // Call function in another module that updates its global variable
  remoteUpdate();
  
  // Read it again
  return remoteGlobal;
};
