// @mode: run
// @result: true

import { Box as BoxA } from './generic_collision_a.zena';
import { Box as BoxB } from './generic_collision_b.zena';

export let main = (): boolean => {
  let a = new BoxA<i32>(42);
  let b = new BoxB<i32>(42);
  return a.value == 42 && b.value == 42;
};
