// Origin of the transparent alias. Its body names `Thing`, which lives
// in the module that imports this one back — so `Step2` genuinely
// crosses an import cycle's back edge, and can only be resolved in this
// module's scope. (No @result: a support module.)
import { Thing } from './iter.zena';

export type Step2 = inline (0, _) | inline (1, Thing);
