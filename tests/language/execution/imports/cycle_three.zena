// @mode: run
// @invoke: main
// @result: 60
// A three-module import cycle: entry -> b -> c -> entry.

import { fromB } from './cycle_three_b.zena';

export let base = (): i32 => 10;

export let main = (): i32 => {
  return fromB() * 2;
};
