// @mode: run
// @result: 8
class Counter {
  var count: i32 = 0;
  new() {}
  increment(n: i32): void {
    this.count += n;
  }
}
export let main = (): i32 => {
  let c = new Counter();
  c.increment(5);
  c.increment(3);
  return c.count;
};
