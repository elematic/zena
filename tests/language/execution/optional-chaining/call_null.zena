// @mode: run
// @result: 1

class Box {
  value: i32 = 0;
  new(v: i32) : value = v;
}

export let main = () => {
  let fn: (() => Box) | null = null;
  let result = fn?();
  if (result == null) { return 1; }
  return 0;
};
