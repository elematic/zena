// @mode: run
// @result: 10

class Point {
  x: i32 = 0;
  y: i32 = 0;
  new(x: i32, y: i32) : x = x, y = y;
}

export let main = () => {
  let p: Point | null = new Point(10, 20);
  return p?.x ?? 0;
};
