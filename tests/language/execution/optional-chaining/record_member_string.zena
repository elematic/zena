// @mode: run
// @result: 5

export let main = () => {
  let p: {name: string} | null = {name: 'hello'};
  let result = p?.name;
  if (result == null) { return 0; }
  let s = result as string;
  return s.length;
};
