// @mode: run
// @result: 1

class Point {
  x: i32 = 0;
  y: i32 = 0;
  new(x: i32, y: i32) { this.x = x; this.y = y; }
}

export let main = () => {
  let p: Point | null = null;
  let result = p?.x;
  if (result == null) { return 1; }
  return 0;
};
