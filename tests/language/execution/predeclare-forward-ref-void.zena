// @mode: run
// @result: 0
// Forward reference to void function with no return type.
let process = (n: i32) => {
  helper(n);
};

let helper = (n: i32) => {
  if (n > 0) {
    process(n - 1);
  }
};

export let main = () => {
  process(5);
  return 0;
};
