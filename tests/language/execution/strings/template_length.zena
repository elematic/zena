// @mode: run
// @result: 1
// A template literal with interpolation, consumed through String
// members (.length) rather than printed.

export let main = (): i32 => {
  let n = 42;
  let s = `n=${n}!`;
  if (s.length == 5) {
    return 1;
  }
  return 0;
};
