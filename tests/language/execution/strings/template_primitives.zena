// @mode: run
// @result: 42, -123, true, false
export let main = (): String => {
  let a: i32 = 42;
  let b: i32 = -123;
  let c: boolean = true;
  let d: boolean = false;
  return `${a}, ${b}, ${c}, ${d}`;
};
