// @mode: run
// @stdout: Hello Return
// A function whose body is a bare string literal returns that string.
// Asserted through stdout because a returned String is a GC reference,
// which `--invoke` cannot render.

let greet = (): String => "Hello Return";

export let main = (): void => {
  console.log(greet());
};
