// @mode: run
// @result: 0

type Point = {x: i32, y: i32};

let getOrigin = (): Point => {
  return {x: 0, y: 0};
};

let checkPoint = (p: Point): i32 => {
  return 0; // Can't test property access yet, just testing argument passing
};

export let main = (): i32 => {
  let p: Point = {x: 10, y: 20};
  let p2 = getOrigin();
  checkPoint(p);
  checkPoint(p2);
  let p3 = p; // Variable assignment
  return 0;
};
