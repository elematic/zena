// @mode: run
// @result: 55

export let main = (): i32 => {
  let arr: FixedArray<i32> = [10, 20, 30];
  let arr2: FixedArray<i32> = [];
  let len = arr.length;
  let len2 = arr2.length;
  let first = arr[0];
  arr[1] = 42;
  let second = arr[1];
  return len + first + second + len2;
};
