// @mode: run
// @result: 6

export let main = (): i32 => {
  let arr = [1, 2, 3, 4, 5];
  var sum = 0;
  for (let x in arr) {
    if (x == 4) {
      break;
    }
    sum = sum + x;
  }
  return sum;
};
