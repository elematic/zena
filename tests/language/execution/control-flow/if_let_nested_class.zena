// @mode: run
// @result: 42

class Point {
  x: i32;
  y: i32;
  new(this.x, this.y);
}

let getPoint = (): inline (boolean, Point) => (true, new Point(40, 2));

export let main = (): i32 => {
  if (let (true, Point {x, y}) = getPoint()) {
    return x + y;
  }
  return 0;
};
