// @mode: run
// @result: -1

let maybeGet = (flag: boolean): inline (boolean, i32) => {
  if (flag) {
    return (true, 100);
  }
  return (false, 0);
};

export let main = (): i32 => {
  if (let (true, v) = maybeGet(false)) {
    return v;
  } else {
    return -1;
  }
};
