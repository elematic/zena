// @mode: run
// @result: 12

export let main = (): i32 => {
  let arr = [1, 2, 3, 4, 5];
  var sum = 0;
  for (let x in arr) {
    if (x == 3) {
      continue;
    }
    sum = sum + x;
  }
  return sum;
};
