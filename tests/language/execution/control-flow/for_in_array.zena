// @mode: run
// @result: 10

export let main = (): i32 => {
  let arr = ["hello" as any, "world" as any];
  var sum = 0;
  for (let x in arr) {
    let s = x as String;
    sum = sum + s.length;
  }
  return sum;
};
