// @mode: run
// @result: 50

type Pair = {first: i32, second: i32};

let getPair = (): inline (boolean, Pair) => (true, {first: 30, second: 20});

export let main = (): i32 => {
  if (let (true, {first, second}) = getPair()) {
    return first + second;
  }
  return 0;
};
