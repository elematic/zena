// @mode: run
// @result: 5

import { ImmutableArray } from 'zena:immutable-array';

export let main = (): i32 => {
  let arr = ["hello" as any] as ImmutableArray<any>;
  var sum = 0;
  for (let x in arr) {
    let s = x as String;
    sum = sum + s.length;
  }
  return sum;
};
