// @mode: run
// @result: 999

let maybeValue = (flag: boolean): inline (true, i32) | inline (false, _) => {
  if (flag) {
    return (true, 999);
  }
  return (false, _);
};

export let main = (): i32 => {
  if (let (true, v) = maybeValue(true)) {
    return v;
  }
  return 0;
};
