// @mode: run
// @result: 10
class Point(x: i32, y: i32)

// Immutable: reading is fine, writing should not be allowed
export let main = (): i32 => {
  let a = new Point(3, 7);
  return a.x + a.y;
};
