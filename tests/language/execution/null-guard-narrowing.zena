// @mode: run
// @result: 42
// Verify null guard narrowing works at runtime

class Wrapper(value: i32)

let process = (x: Wrapper | null): i32 => {
  if (x == null) { return 0; }
  return x.value + 1;
};

export let main = (): i32 => {
  return process(new Wrapper(41));
};
