// @mode: run
// @result: 42

let getResult = (): inline (i32, boolean) => (42, true);

let test = (): i32 => {
  return if (let (value, true) = getResult()) {
    value
  } else {
    0
  };
};

export let main = (): i32 => test();
