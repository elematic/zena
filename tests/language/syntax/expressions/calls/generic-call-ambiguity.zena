// @target: expression
// A comparison expression that happens to have balanced angle brackets followed by parenthesis
// It should NOT be parsed as a generic call.
(value < 10) && (other > 5) || (next == temp);