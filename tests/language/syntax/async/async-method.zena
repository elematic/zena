// @target: statement
class Client {
  async get(url: String): Future<String> {
    return await this.send(url);
  }
  static async connect(host: String): Future<Client> {
    return await dial(host);
  }
}
