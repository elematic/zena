// @target: statement
// `using` declares the binding itself, so `let` is not written. The spelling
// is rejected rather than accepted as a synonym, so there is one way to write
// it. See docs/design/ownership.md §`using`.
// @error: already declares the binding
using let file = open(path);
