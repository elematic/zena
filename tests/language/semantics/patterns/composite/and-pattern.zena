// @mode: check
// AND patterns accumulate bindings from both branches
class Point {
  x: i32;
  y: i32;
  new(this.x, this.y);
}

let p = new Point(1, 2);
let s = match (p) {
  case Point {x} & Point {y}: x + y
  case _: 0
};
let n: i32 = s;
