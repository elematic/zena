// @mode: check
// Class pattern binds fields with their declared types in match arms
class Point {
  x: i32;
  y: i32;
  new(this.x, this.y);
}

let p = new Point(1, 2);
let r = match (p) {
  case Point {x, y}: x + y
};
let n: i32 = r;
