// @mode: check
// Refutable class patterns are not allowed in for-in declarations
sealed class Option {
  case Some(value: i32)
  case None
}

let items = [None];
for (let Some {value} in items) { // @error: Refutable pattern
  let n: i32 = value;
}
