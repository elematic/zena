// @mode: check
// Nested class patterns bind nested fields with the right types
class Point {
  x: i32;
  y: i32;
  new(this.x, this.y);
}

class Rect {
  p: Point;
  new(this.p);
}

let r = new Rect(new Point(5, 7));
let out = match (r) {
  case Rect {p: Point {x, y}}: x + y
};
let n: i32 = out;
