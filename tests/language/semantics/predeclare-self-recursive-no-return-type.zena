// @mode: check
// Self-recursive function with all params annotated but no return type.
// With lazy resolution, self-recursion requires explicit return type annotation.
let countdown = (n: i32) => { // @error: Recursive function requires an explicit return type annotation
  if (n > 0) {
    countdown(n - 1);
  }
};
