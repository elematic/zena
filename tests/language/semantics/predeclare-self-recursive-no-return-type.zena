// @mode: check
// Self-recursive function with all params annotated but no return type.
// Should be pre-declared so the self-call resolves.
let countdown = (n: i32) => {
  if (n > 0) {
    countdown(n - 1);
  }
};
