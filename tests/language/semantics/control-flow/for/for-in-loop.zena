// @mode: check
// for-in loop with Array<i32>
import {GrowableArray} from 'zena:growable-array';
let items: GrowableArray<i32> = new GrowableArray<i32>();
for (let item in items) {
  let x: i32 = item;
}
