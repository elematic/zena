// @mode: check
// if-let with identifier pattern binds the value in consequent
sealed class MyOption {
  case MySome(value: i32)
  case MyNone
}
let maybe: MyOption = new MySome(42);
if (let x = maybe) {
  let y: MyOption = x;
}
