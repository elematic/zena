// @mode: check
// A bare name in a pattern must name a class. A name that resolves to some
// other kind of type is reported rather than silently becoming a binding.
type Pair = {a: i32, b: i32};

let describe = (p: Pair): String => match (p) {
  case Pair: 'pair' // @error: only classes can be matched by name
};
