// @mode: check
// A case with a guard is non-exhaustive even if the pattern would be exhaustive
let x: boolean = true;
// @error: Non-exhaustive match. Not all cases are covered.
match (x) {
  case true if x: 1
  case false: 2
};
