// @mode: check
// while-let binds fields with their declared types from the matched variant in the loop body.

sealed class Stream {
  case Item(value: i32)
  case Done
}

let getNext = (): Stream => new Done();
var sum: i32 = 0;
while (let Item {value} = getNext()) {
  sum += value;
}
