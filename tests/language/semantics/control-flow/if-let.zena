// @mode: check
// if-let with class pattern binds variables in the consequent branch
sealed class Result {
  case Ok(value: i32)
  case Err
}
let maybe: Result = new Ok(42);
if (let Ok = maybe) {
  let x: i32 = 1;
}
