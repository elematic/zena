// @mode: check
// while-let with class pattern binds variables in the loop body
sealed class Result {
  case Ok(value: i32)
  case Err
}
let getNext = (): Result => new Err();
while (let Ok = getNext()) {
  let x: i32 = 1;
}
