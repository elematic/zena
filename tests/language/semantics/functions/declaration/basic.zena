// @mode: check
// A `function` declaration binds a name to a function value, exactly like
// an annotated arrow, and is usable as a value.
function add(a: i32, b: i32): i32 {
  return a + b;
}

let sum: i32 = add(1, 2);
let asValue: (a: i32, b: i32) => i32 = add;
