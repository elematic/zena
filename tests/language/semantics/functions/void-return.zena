// @mode: check
// Functions that return void.

// Explicit void annotation
let f = (): void => {};

// Block body with no return
let g = (): void => {
  let x = 1;
};

// Calling void functions
f();
g();

// Void function cannot be used as a value
let x: i32 = f();  // @error: Type mismatch
