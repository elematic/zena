// @mode: check
// Forward reference: a function can call another function defined later in the file.
let main = (n: i32) => {
  helper(n);
};

let helper = (n: i32) => {
  if (n > 0) {
    // Just do something, don't call 'main' back
    let x = n * 2;
  }
};
