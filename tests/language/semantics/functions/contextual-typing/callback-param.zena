// @mode: check
// Callback parameter types are inferred from the callee's function type.

let apply = (fn: (x: i32) => i32, value: i32): i32 => fn(value);

// x is inferred as i32 from the fn parameter type
let r: i32 = apply((x) => x * 2, 5);
