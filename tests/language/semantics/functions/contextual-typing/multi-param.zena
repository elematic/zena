// @mode: check
// Multiple callback parameters are inferred from the callee's function type.

let fold = (fn: (acc: i32, x: i32) => i32, init: i32, a: i32, b: i32): i32 =>
  fn(fn(init, a), b);

// Both acc and x are inferred as i32
let r: i32 = fold((acc, x) => acc + x, 0, 3, 4);
