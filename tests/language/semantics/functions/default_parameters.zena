// @mode: check

let f = (a: i32, b: i32 = 0) => a + b;

f(1, 2);
f(1); // Should not error

let g = (a: i32 = 1, b: i32 = 2) => a + b;
g();
g(1);
g(1, 2);

f(); // @error: Expected 1-2 arguments, got 0
f(1, 2, 3); // @error: Expected 1-2 arguments, got 3
