// @mode: check
// Mutually recursive functions without return type annotations are an error.
let caller = (n: i32) => {
  helper(n);
};

let helper = (n: i32) => { // @error: Recursive function requires an explicit return type annotation
  if (n > 0) {
    caller(n - 1);
  }
};
