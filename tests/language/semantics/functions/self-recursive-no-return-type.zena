// @mode: check
// Self-recursive function without a return type annotation is an error.
let countdown = (n: i32) => { // @error: Recursive function requires an explicit return type annotation
  if (n > 0) {
    countdown(n - 1);
  }
};
