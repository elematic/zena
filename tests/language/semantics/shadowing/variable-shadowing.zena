// @mode: check
// An inner scope can shadow an outer variable with a different type.
let test = (): void => {
  let x: i32 = 1;
  let inner = (): i32 => {
    let x: i32 = 99; // shadows outer x
    return x;
  };
  let n: i32 = x; // outer x still has type i32
};
