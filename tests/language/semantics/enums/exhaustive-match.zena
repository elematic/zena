// @mode: check
// Match over enum members is exhaustive when all members are covered
enum Color { Red, Green, Blue }
let name = (c: Color): String => match (c) {
  case Color.Red: 'red'
  case Color.Green: 'green'
  case Color.Blue: 'blue'
};
