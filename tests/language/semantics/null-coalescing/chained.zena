// @mode: check

class Box {
  value: i32 = 0;
  new(value: i32) { this.value = value; }
}

export let main = () => {
  let a: Box | null = null;
  let b: Box | null = null;
  let result: Box = a ?? b ?? new Box(0);
};
