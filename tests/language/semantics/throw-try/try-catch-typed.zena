// @mode: check
import {Error} from 'zena:error';
// The catch parameter has type Error and can be re-thrown.
let test = () => {
  try {
    throw new Error('oops');
  } catch (e) {
    throw e;
  };
};
