// @mode: check
import {Error} from 'zena:error';
// A try/catch/finally block is valid; the finally block is type-checked but
// does not affect the overall expression type.
let test = () => {
  try {
    throw new Error('oops');
  } catch (e) {
    // handle
  } finally {
    // cleanup
  };
};
