// @mode: check
// Mutual recursion with return type annotations (required for cycles).
let isEven = (n: i32): boolean => {
  if (n == 0) { return true; }
  return isOdd(n - 1);
};

let isOdd = (n: i32): boolean => {
  if (n == 0) { return false; }
  return isEven(n - 1);
};
