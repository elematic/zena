// @mode: check
// A generator's declared return type is the call expression's type:
// it must be Iterator<T>, not the yield type.
let g = gen (): i32 => { // @error: A generator's return type must be 'Iterator<T>'
  yield 1;
};
