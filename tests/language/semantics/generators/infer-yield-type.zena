// @mode: check
// Without an annotation the yield type is inferred from the yield
// statements; the module needs no imports for the Iterator wrapping.
let g = gen () => {
  yield 42;
};

export let use = (): void => {
  for (let x in g()) {
    let _y: i32 = x;
  }
};
