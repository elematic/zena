// @mode: check
// A non-gen closure nested in a generator is its own function; it
// cannot suspend its lexical parent.
let g = gen (): Iterator<i32> => {
  let inner = (): void => {
    yield 1; // @error: 'yield' is only valid inside the body of a 'gen' function
  };
  inner();
  yield 2;
};
