// @mode: check

interface Handler<V> {
  handle(value: V): void;
}

class StringHandler implements Handler<String> {
  handle(value: String): void {}
}

class NumberHandler implements Handler<i32> { // @error: incorrectly implements interface
  // Missing implementation or wrong return type catches substitution fixes
  handle(value: String): void {}
}
