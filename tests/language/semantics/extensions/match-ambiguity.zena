// @mode: check
// Ambiguous extension match cases should fail

class MyType {
  new() {}
}

extension class ExtA on MyType {}
extension class ExtB on MyType {}

let test = (x: MyType) => {
  match (x) {
    case ExtA {}: {} // @error: Ambiguous match case
    case ExtB {}: {}
  };
};
