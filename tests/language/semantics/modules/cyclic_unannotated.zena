// @mode: check
// A function crossing an import cycle needs a fully written signature;
// inference has no stable order across the cycle.
// @error: a function crossing an import cycle needs an explicit signature
import { probe } from './cyclic_unannotated_first.zena';

export let plain = (n: i32) => n + 1;
export let touch = (): i32 => probe();
