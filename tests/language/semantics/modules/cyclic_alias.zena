// @mode: check
// A transparent type alias crosses an import cycle's back edge, resolved
// as an identity-bearing shell filled in by its origin — the same way
// nominal types cross (docs/design/import-cycles.md, restriction lifted).
// No diagnostic: this file asserts the alias `Pair` and the cyclic
// import both check clean.
import { probe } from './cyclic_alias_first.zena';

export type Pair = (i32, i32);
export let touch = (): i32 => probe();
