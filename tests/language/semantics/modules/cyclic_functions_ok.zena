// @mode: check
// Import cycles between fully annotated functions are legal.
import { pong } from './cyclic_functions_ok_first.zena';

export let ping = (n: i32): i32 => {
  if (n <= 0) { return 0; }
  return pong(n - 1) + 1;
};
export let touch = (): i32 => ping(4);
