// @mode: check
// Types cannot cross an import cycle's back edge.
// @error: Cyclic import of type 'Thing'
import { probe } from './cyclic_type_first.zena';

export class Thing {
  x: i32 = 1;
}
export let touch = (): i32 => probe();
