// @mode: check
// Destructuring can mix required fields and optional fields with defaults
let request = (opts: {url: String, timeout?: i32, retries?: i32}) => {
  let {url, timeout = 30000, retries = 3} = opts;
  let sum: i32 = timeout + retries;
  let u: String = url;
  return sum;
};
