// @mode: check
// Destructuring an optional field in an irrefutable position requires
// a default value; presence tests belong to if-let and match.
let process = (opts: {timeout?: i32}) => {
  let {timeout} = opts; // @error: requires a default value when destructuring
  return 0;
};
