// @mode: check
// An as-pattern narrows the binding to the matched class type.

sealed class Shape {
  case Circle(radius: f64)
  case Rect(w: f64, h: f64)
}

let describe = (s: Shape): f64 => match (s) {
  case Circle {} as c: c.radius
  case Rect {} as r: r.w * r.h
};
