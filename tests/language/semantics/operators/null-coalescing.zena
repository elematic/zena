// @mode: check
// The ?? operator returns the left operand if non-null, otherwise the right.

// ?? removes null from the result type
let a: String | null = null;
let b: String = a ?? 'default';

// ?? with chained usage
let c: String | null = null;
let d: String = a ?? c ?? 'fallback';
