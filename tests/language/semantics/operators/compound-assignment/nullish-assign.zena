// @mode: check
// ??= assigns when null, narrows type, and rejects non-nullable LHS
class Box(value: i32)

// Valid: ??= on nullable var
let withNullable = (): void => {
  var x: Box | null = null;
  x ??= new Box(42);
  // x is narrowed to Box after ??=
  let v: i32 = x.value;
};
