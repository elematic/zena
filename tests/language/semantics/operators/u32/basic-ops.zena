// @mode: check
// u32 arithmetic, comparison, and bitwise operations
let compute = (a: u32, b: u32) => {
  let sum = a + b;
  let diff = a - b;
  let prod = a * b;
  let quot = a / b;
  let rem = a % b;
};
