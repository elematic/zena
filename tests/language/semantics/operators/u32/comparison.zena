// @mode: check
// u32 comparison operations
let compare = (a: u32, b: u32): boolean => {
  let lt = a < b;
  let le = a <= b;
  let gt = a > b;
  let ge = a >= b;
  let eq = a == b;
  let ne = a != b;
  return lt;
};
