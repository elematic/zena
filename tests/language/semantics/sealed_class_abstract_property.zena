// @mode: check

class SourceLocation(line: i32)

sealed class Node {
  case Expression
  abstract loc?: SourceLocation;
}

sealed class Expression extends Node {
  case Identifier(name: String, loc?: SourceLocation)
}

let getLoc = (e: Expression): SourceLocation | null => e.loc;
