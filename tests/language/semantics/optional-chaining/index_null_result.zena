// @mode: check

class Items {
  data: i32 = 0;
  operator [](index: i32): i32 { return this.data; }
}

export let main = () => {
  let arr: Items | null = null;
  let bad = arr?[0]; // @error: coalescence
  let good = arr?[0] ?? -1;
  if (good == -1) { return 1; }
  return 0;
};
