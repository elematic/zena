// @mode: check

class Items {
  data: i32 = 0;
  operator [](index: i32): i32 { return this.data; }
}

export let main = () => {
  let arr: Items | null = null;
  let elem = arr?[0];  // Should infer i32 | null
  if (elem == null) { return 1; }
  return 0;
};
