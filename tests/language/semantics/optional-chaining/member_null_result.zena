// @mode: check

class Point {
  x: i32 = 0;
  new(x: i32) : x = x;
}

export let main = () => {
  let p: Point | null = null;
  let bad = p?.x; // @error: coalescence
  let good = p?.x ?? -1;
  if (good == -1) { return 1; }
  return 0;
};
