// @mode: check

class Point {
  x: i32 = 0;
  new(x: i32) { this.x = x; }
}

export let main = () => {
  let p: Point | null = null;
  let result = p?.x;  // Should infer i32 | null
  if (result == null) { return 1; }
  return 0;
};
