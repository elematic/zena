// @mode: check

export let main = () => {
  let fn: ((a: i32) => i32) | null = null;
  let bad = fn?(42); // @error: coalescence
  let good = fn?(42) ?? -1;
  if (good == -1) { return 1; }
  return 0;
};
