// @mode: check
// Pure forward reference: main calls helper, helper doesn't call back.
// No cycle, so no return type annotation needed.
let main = (n: i32) => {
  helper(n);
};

let helper = (n: i32) => {
  if (n > 0) {
    // Just do something, don't call 'main' back
    let x = n * 2;
  }
};
