// @mode: check

// The other route to the same private member: name the unexported
// interface. It is not imported and not exported, so the qualifier has
// nothing to resolve to.
import { Holder } from './lib.zena';

let test = (): void => {
  let h = new Holder(41);
  let forged: i32 = h.[Secret.peek](); // @error: Type 'Secret' not found
};
