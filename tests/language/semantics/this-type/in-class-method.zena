// @mode: check
class Point {
  x: i32;
  y: i32;
  new(x: i32, y: i32) : x = x, y = y {}
  equals(other: this): boolean {
    return this.x == other.x && this.y == other.y;
  }
}
let p1 = new Point(1, 2);
let p2 = new Point(1, 2);
let result: boolean = p1.equals(p2);
