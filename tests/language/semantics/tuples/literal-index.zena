// @mode: check
// Tuple indexing with literal indices returns the specific element type.

let t = (42, "hello", true);

// Literal index returns specific element type.
let a: i32 = t[0];
let b: String = t[1];
let c: boolean = t[2];
