// @mode: check

class Point {
  coords: inline (i32, i32); // @error: Inline tuple types can only appear in function return types, not here.
  new() {}
}

interface HasCoords {
  coords: inline (i32, i32); // @error: Inline tuple types can only appear in function return types, not here.
}

class Box<T> {
  value: T;
  new(this.value);
}

export let main = (): i32 => {
  let x: inline (i32, i32) = (1, 2); // @error: Inline tuple types can only appear in function return types, not here.
  let arr: array<inline (i32, i32)> = []; // @error: Inline tuple types can only appear in function return types, not here.
  let f = (y: inline (i32, i32)): i32 => 0; // @error: Inline tuple types can only appear in function return types, not here.
  let b: Box<inline (i32, i32)> = new Box((1, 2)); // @error: Inline tuple types can only appear in function return types, not here.
  return 0;
};
