// @mode: check
// Generic type argument inference from constructor arguments.

class Box<T> {
  value: T;
  new(this.value);
}

// Direct inference: T inferred from argument type.
let b1 = new Box(42);
let v1: i32 = b1.value;

let b2 = new Box("hello");
let v2: String = b2.value;
