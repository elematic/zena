// @mode: check
// Generic function call inference: infer T from argument types.

let identity = <T>(x: T): T => x;

let a: i32 = identity(42);
let b: String = identity("hello");
let c: boolean = identity(true);
