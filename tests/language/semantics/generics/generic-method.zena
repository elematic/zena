// @mode: check
class Utils {
  identity<T>(val: T): T {
    return val;
  }
}

let u = new Utils();
let x: i32 = u.identity<i32>(42);
let y: String = u.identity<String>("hello");
let z: i32 = u.identity<String>("bad"); // @error: Type mismatch
