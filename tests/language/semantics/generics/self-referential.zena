// @mode: check
class Node<T> {
  value: T;
  next: Node<T> | null;
  new(this.value, this.next);
}

let node2: Node<i32> = new Node<i32>(2, null);
let node1: Node<i32> = new Node<i32>(1, node2);
let bad: Node<String> = new Node<String>("hi", node1); // @error: Type mismatch
