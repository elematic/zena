// @mode: check
class Box<T> {
  #val: T;
  new(val: T) {
    this.#val = val;
  }

  getVal(): T {
    return this.#val;
  }
}

let b = new Box<i32>(42);
let x: i32 = b.getVal();
let y: i32 = b.#val; // @error: can only be accessed within a class
