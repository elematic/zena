// @mode: check
// Fully-annotated mutual recursion (existing behavior, should still work).
let isEven = (n: i32): boolean => {
  if (n == 0) { return true; }
  return isOdd(n - 1);
};

let isOdd = (n: i32): boolean => {
  if (n == 0) { return false; }
  return isEven(n - 1);
};
