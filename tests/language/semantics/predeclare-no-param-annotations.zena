// @mode: check
// Function with no param annotations should NOT be pre-declared.
// This is valid code — it just can't support forward references.
let add = (a, b) => a + b; // @error: no type annotation
