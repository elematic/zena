// @mode: check

import { MyAlias, SimpleAlias } from './lib.zena';

let test = (): void => {
  let a: SimpleAlias = "string"; // @error: Type mismatch
  
  let b: MyAlias<String> = { value: "hello", next: null };
  let c: MyAlias<i32> = b; // @error: Type mismatch
};