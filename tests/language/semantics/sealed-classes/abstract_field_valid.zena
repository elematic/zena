// @mode: check
// Abstract field should type-check correctly
sealed class Shape {
  case Circle, Rect
  abstract area: f64;
}
class Circle(radius: f64, area: f64) extends Shape
class Rect(width: f64, height: f64, area: f64) extends Shape

let getArea = (s: Shape): f64 => s.area;
