// @mode: check
// Non-exhaustive match on sealed class reports error
sealed class Shape {
  case Circle(radius: f64)
  case Rect(width: f64, height: f64)
}
let describe = (s: Shape): String => match (s) { // @error: Non-exhaustive match
  case Circle {}: "circle"
};
