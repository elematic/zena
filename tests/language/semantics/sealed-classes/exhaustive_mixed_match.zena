// @mode: check
// Exhaustive match with mixed unit and class variants
sealed class Token {
  case Number(value: i32)
  case Plus, Minus
}
let describe = (t: Token): string => match (t) {
  case Number {}: "num"
  case Plus: "plus"
  case Minus: "minus"
};
