// @mode: check
// Exhaustive match on unit variants with identifier patterns
sealed class Color {
  case Red, Green, Blue
}
let toStr = (c: Color): String => match (c) {
  case Red: "red"
  case Green: "green"
  case Blue: "blue"
};
