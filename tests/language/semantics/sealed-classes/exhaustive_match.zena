// @mode: check
// Exhaustive match on sealed class with class patterns
sealed class Shape {
  case Circle(radius: f64)
  case Rect(width: f64, height: f64)
}
let describe = (s: Shape): String => match (s) {
  case Circle {}: "circle"
  case Rect {}: "rect"
};
