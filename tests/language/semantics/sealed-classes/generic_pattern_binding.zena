// @mode: check
// ClassPattern destructuring resolves type parameters in match
sealed class Result {
  case Ok(value: i32)
  case Err(msg: String)
}
let r: Result = new Ok(42);
let result: i32 = match (r) {
  case Ok {value}: value
  case Err {msg}: 0
};
