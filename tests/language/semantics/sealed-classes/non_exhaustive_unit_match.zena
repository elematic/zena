// @mode: check
// Non-exhaustive match on unit variants reports error
sealed class Color {
  case Red, Green, Blue
}
let toStr = (c: Color): string => match (c) { // @error: Non-exhaustive match
  case Red: "red"
  case Green: "green"
};
