// @mode: check
// Wildcard pattern makes sealed match exhaustive
sealed class Shape {
  case Circle(radius: f64)
  case Rect(width: f64, height: f64)
}
let describe = (s: Shape): string => match (s) {
  case Circle {}: "circle"
  case _: "other"
};
