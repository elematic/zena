// @mode: check
import {Error} from 'zena:error';

// never is assignable to any type.
let fail = (): never => {
  throw new Error("never");
};

let test = () => {
  let x: i32 = fail();
  let y: String = fail();
  let z: boolean = fail();
};
