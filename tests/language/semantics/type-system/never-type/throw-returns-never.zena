// @mode: check
import {Error} from 'zena:error';

// throw is an expression with type `never`.
let fail = (): never => throw new Error("fail");

let test = () => {
  // never is assignable to `never`
  let x: never = throw new Error("oops");
};
