// @mode: check
// Pure forward reference with return value: getDoubled calls double.
// No cycle, no annotation needed, return type inferred.
let getDoubled = (n: i32) => double(n);

let double = (n: i32) => n * 2;
