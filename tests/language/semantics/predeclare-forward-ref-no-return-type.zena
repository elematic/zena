// @mode: check
// Forward reference: caller defined before callee, callee has no return type.
// Both have all params annotated.
let caller = (n: i32) => {
  helper(n);
};

let helper = (n: i32) => {
  if (n > 0) {
    caller(n - 1);
  }
};
