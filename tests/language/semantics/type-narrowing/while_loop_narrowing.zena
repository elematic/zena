// @mode: check

class Item(val: String)

let testWhile = (x: Item | null): String => {
  while (x != null) {
    // x should be narrowed to Item inside the loop body
    return x.val;
  }
  return "";
};
