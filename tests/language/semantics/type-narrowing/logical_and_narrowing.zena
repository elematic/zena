// @mode: check

class Inner(value: String)
class Outer(inner: Inner | null)

let testAnd = (x: Outer | null): String => {
  if (x != null && x.inner != null) {
    // Both x and x.inner should be narrowed to non-null
    return x.inner.value;
  }
  return "";
};
