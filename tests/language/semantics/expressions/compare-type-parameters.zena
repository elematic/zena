// @mode: check

let greater = <T>(a: T, b: T): boolean => {
  return a > b;
};

let less = <T>(a: T, b: T): boolean => {
  return a < b;
};

let gte = <T>(a: T, b: T): boolean => {
  return a >= b;
};

let lte = <T>(a: T, b: T): boolean => {
  return a <= b;
};
