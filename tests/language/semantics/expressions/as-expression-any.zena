// @mode: check
// As expression can downcast from superclass
class Animal {
  name: String;
  new(this.name);
}
class Dog extends Animal {
  new(name: String) : super(name);
}
let a: Animal = new Dog('Rex');
let d = a as Dog;
