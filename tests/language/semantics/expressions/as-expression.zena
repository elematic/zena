// @mode: check
// As expression casts between compatible types
class Animal {
  name: String;
  new(this.name);
}
class Dog extends Animal {
  new(name: String) : super(name);
}
let a: Animal = new Dog('Rex');
let d = a as Dog;
