// @mode: run
// @result: 0

export type CycleValue = CycleObject | String;

export class CycleObject {
  forEach(callback: (key: String, value: CycleValue) => void): void {
     // do nothing
  }
}

export let main = (): i32 => {
  let expectedObj = new CycleObject();
  let myCallback = (key: String, value: CycleValue) => {
      // should compile
  };
  expectedObj.forEach(myCallback);
  return 0;
};
