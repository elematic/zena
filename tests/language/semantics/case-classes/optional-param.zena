// @mode: check

class Person(name: String, email?: String)

let p1 = new Person("Alice", "alice@example.com");
let p2 = new Person("Bob");

let a: String = p1.email; // Wait, shouldn't email be String | null or String? Let's check. Wait, if it's not provided, what is its type?
let b: String | null = p1.email; 
let n: String = p1.name;

let p3 = new Person(); // @error: Expected 1-2 arguments
