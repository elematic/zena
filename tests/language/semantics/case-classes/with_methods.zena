// @mode: check
// Case class with a body should allow additional methods
class Point(x: i32, y: i32) {
  sum(): i32 {
    return this.x + this.y;
  }
}

let p = new Point(3, 4);
let s: i32 = p.sum();
