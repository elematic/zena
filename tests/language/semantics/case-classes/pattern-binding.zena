// @mode: check

class Point(x: i32, y: i32)

let p = new Point(10, 20);

let result = match (p) {
  case Point {x, y}: x + y
};

let b1: i32 = result;
let b2: String = result; // @error: Type mismatch
