// @mode: check
// @error: Cannot extend case class 'Point'. Case classes are implicitly final.
class Point(x: i32, y: i32)

class FakePoint extends Point {
  new(x: i32, y: i32) {
    super(x, y);
  }
}
