// @mode: check
// Inferred types from literals
let a = 10;       // inferred i32
let b = 3.14;     // inferred f32
let c = true;     // inferred true (literal type)
let d = false;    // inferred false (literal type)
