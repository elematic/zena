// @mode: check

class Base {
  #secret: i32 = 42;
}

mixin M on Base {
  #mixinSecret: i32 = 100;

  getSecret(): void {
    let y = this.#mixinSecret; // Should succeed
    let x = this.#secret; // @error: Property '#secret' does not exist on type 'M_This'.
  }
}
