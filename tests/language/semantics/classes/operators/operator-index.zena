// @mode: check

class MyArray<T> {
  new();

  operator [](index: i32): T {
    throw new Error("stub");
  }
}

let arr = new MyArray<String>();
let item = arr[0];

let b1: String = item;
let b2: i32 = item; // @error: Type mismatch

let c = arr["string"]; // @error: Type mismatch
