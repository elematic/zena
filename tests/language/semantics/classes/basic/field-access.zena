// @mode: check
// Valid class with field access and method call
class Point {
  x: i32;
  y: i32;
  new(x: i32, y: i32) : x = x, y = y {}
  distance(): i32 {
    return 0;
  }
}
let p = new Point(1, 2);
let x = p.x;
p.distance();
