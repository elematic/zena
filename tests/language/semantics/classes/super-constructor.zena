// @mode: check
// Super constructor call in subclass
class Base {
  x: i32;
  new(this.x);
}
class Child extends Base {
  y: i32;
  new(x: i32, this.y) : super(x);
}
let c = new Child(1, 2);
let v: i32 = c.x;
