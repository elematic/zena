// @mode: check
// Private field cannot be accessed outside the class
class Counter {
  var #count: i32 = 0;
  increment(): void {
    this.#count += 1;
  }
  value(): i32 {
    return this.#count;
  }
}
let c = new Counter();
let x = c.#count; // @error: Private field
