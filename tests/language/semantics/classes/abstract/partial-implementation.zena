// @mode: check
// An abstract subclass may partially implement abstract methods
abstract class Shape {
  abstract area(): i32;
  abstract perimeter(): i32;
}
abstract class RegularPolygon extends Shape {
  // implements perimeter but leaves area abstract — valid for abstract class
  perimeter(): i32 {
    return 0;
  }
}
