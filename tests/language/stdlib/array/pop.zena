// @result: 100
import {Array} from 'zena:array';

export let main = (): i32 => {
  let arr = new Array<i32>(4);
  arr.push(10);
  arr.push(20);
  let v1 = arr.pop();
  if (v1 != 20) return 1;
  if (arr.length != 1) return 2;
  let v2 = arr.pop();
  if (v2 != 10) return 3;
  if (arr.length != 0) return 4;
  return 100;
};
