// Array sum: fixed array literal of three numbers, summed by a for-in loop.
// Exercises collection instantiation, generic specialization, and iteration.
export function main(): i32 {
  let xs = [1, 2, 3];
  var total = 0;
  for (let x in xs) {
    total += x;
  }
  return total;
}
