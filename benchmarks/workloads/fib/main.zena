// Recursive fib(27) using top-level function declarations.
function fib(n: i32): i32 {
  return if (n < 2) { n } else { fib(n - 1) + fib(n - 2) };
}

export function main(): i32 {
  return fib(27);
}
