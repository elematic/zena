// Iterative arithmetic loop: branch + integer codegen quality, no calls, no allocation.
export let main = (): i32 => {
  var sum = 0;
  var i = 0;
  while (i < 5000000) {
    if (i % 3 == 0) {
      sum += i % 7;
    } else {
      sum -= i % 5;
    }
    i += 1;
  }
  return sum % 256;
};
