// clock.zena, spelled through a WIT-typed module: the import's
// signature comes from the package's WIT rather than a transcribed
// @external, and the emitted component should agree with clock.zena's
// section for section.
import { now } from 'wasi:clocks/monotonic-clock';

export function main(): u64 {
  return now();
}
