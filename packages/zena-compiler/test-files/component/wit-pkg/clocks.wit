// The clocks interface as a WIT-backed package for the WIT-typed-module
// tests: the alias-typed result is what the real interface declares,
// and the synthesized declaration resolves it to u64.
package wasi:clocks@0.3.0;

interface monotonic-clock {
  type instant = u64;

  now: func() -> instant;
}
