// A core-module import namespace. A component imports WIT interfaces, so
// this must fail to compile rather than emit a component that cannot be
// instantiated by anything.
@external("env", "doThing")
declare function doThing(): i32;

export function main(): i32 {
  return doThing();
}
