// A String crosses the boundary as a `list<u8>` through linear memory,
// which needs the canonical ABI's memory options — C3. Until then this
// must fail by name rather than emit an export nothing can call.
export function main(): String {
  return "hi";
}
