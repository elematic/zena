import { Iterator } from 'zena:iterator';

let counting = gen (): Iterator<i32> => {
  yield 1;
  yield 2;
};

export let main = (): i32 => {
  var acc = 0;
  for (let v in counting()) {
    acc += v;
  }
  return acc - 3;
};
