// Imports a library that re-exports TemplateStringsArray, and contains no
// tagged template. Nothing here can build one, so none of it should ship.
import { StringBuilder } from 'zena:core';

export function main(): i32 {
  let s = 'hello';
  return s.length;
}
