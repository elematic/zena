// The smallest program Zena can compile: no strings, no collections,
// no allocation, no calls. Whatever ends up in its module got there
// without the program asking. `binary-size_test.zena` holds it to a
// byte budget; see docs/design/binary-size.md.
export function main(): i32 { return 42; }
