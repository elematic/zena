package root:component;

world root {
  import config: interface {
    get-flag: func(name: string) -> option<string>;
  }
  import log: func(level: u8, message: string);
  import fetch-bytes: func(url: string) -> result<list<u8>, string>;
  import now: func() -> u64;
}
