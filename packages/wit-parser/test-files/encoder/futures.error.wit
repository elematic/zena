// Futures and streams have no runtime until C6, and the encoder says so
// rather than emitting types nothing can lower. The harness asserts the
// failure and its message.
package test:events@0.1.0;

interface events {
  poll: func() -> stream<u8>;
}

world event-host {
  import events;
}
