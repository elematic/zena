// GENERATED by scripts/generate-wpt-url-tests.js — DO NOT EDIT.
// Source: web-platform-tests/wpt url/resources/setters_tests.json
// Pinned at commit fefb5fc613b29f75ba402dc18360f98c7e92b212. See tests/url/wpt/README.md.
//
// The web's mutable setters map to zena:url's with* methods, which
// return a new URL. Cases whose "expected" repeats the original
// components are the spec rejecting the value by leaving the URL
// alone, which is exactly what these pin.
import {suite, test, testSkip, TestContext} from 'zena:test';
import {equal, isTrue} from 'zena:assert';
import {URL} from 'zena:url';
import { StringBuilder } from 'zena:core';

// Decodes the %XX escapes this generator uses for control bytes that
// Zena string literals cannot express. Local on purpose: the harness
// must not depend on zena:url's own percent-decoder.
let hexVal = (b: i32): i32 => {
  if (b >= 0x30 && b <= 0x39) return b - 0x30;
  if (b >= 0x41 && b <= 0x46) return b - 0x41 + 10;
  return b - 0x61 + 10;
};

let dec = (s: String): String => {
  let sb = new StringBuilder(s.length);
  var i = 0;
  while (i < s.length) {
    if (s.getByteAt(i) == 0x25 && i + 2 < s.length) {
      sb.appendByte((hexVal(s.getByteAt(i + 1)) << 4) | hexVal(s.getByteAt(i + 2)));
      i += 3;
    } else {
      sb.appendByte(s.getByteAt(i));
      i += 1;
    }
  }
  return sb.toString();
};

export let tests = suite('WPT setters', (): void => {
  test('#0 protocol= on a://example.net — The empty string is not a valid scheme. Setter leaves the URL unchanged.', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('');
      equal(out.href, 'a://example.net');
      equal(out.protocol, 'a:');
    }
  });
  test('#1 protocol=b on a://example.net', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('b');
      equal(out.href, 'b://example.net');
      equal(out.protocol, 'b:');
    }
  });
  test('#2 protocol=defuse on javascript:alert(1)', (ctx: TestContext): void => {
    let u = URL.parse('javascript:alert(1)', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('defuse');
      equal(out.href, 'defuse:alert(1)');
      equal(out.protocol, 'defuse:');
    }
  });
  test('#3 protocol=B on a://example.net — Upper-case ASCII is lower-cased', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('B');
      equal(out.href, 'b://example.net');
      equal(out.protocol, 'b:');
    }
  });
  test('#4 protocol=é on a://example.net — Non-ASCII is rejected', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('é');
      equal(out.href, 'a://example.net');
      equal(out.protocol, 'a:');
    }
  });
  test('#5 protocol=0b on a://example.net — No leading digit', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('0b');
      equal(out.href, 'a://example.net');
      equal(out.protocol, 'a:');
    }
  });
  test('#6 protocol=+b on a://example.net — No leading punctuation', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('+b');
      equal(out.href, 'a://example.net');
      equal(out.protocol, 'a:');
    }
  });
  test('#7 protocol=bC0+-. on a://example.net', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('bC0+-.');
      equal(out.href, 'bc0+-.://example.net');
      equal(out.protocol, 'bc0+-.:');
    }
  });
  test('#8 protocol=b,c on a://example.net — Only some punctuation is acceptable', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('b,c');
      equal(out.href, 'a://example.net');
      equal(out.protocol, 'a:');
    }
  });
  test('#9 protocol=bé on a://example.net — Non-ASCII is rejected', (ctx: TestContext): void => {
    let u = URL.parse('a://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('bé');
      equal(out.href, 'a://example.net');
      equal(out.protocol, 'a:');
    }
  });
  test('#10 protocol=file on http://test@example.net — Can’t switch from URL containing username/password/port to file', (ctx: TestContext): void => {
    let u = URL.parse('http://test@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('file');
      equal(out.href, 'http://test@example.net/');
      equal(out.protocol, 'http:');
    }
  });
  test('#11 protocol=file on https://example.net:1234', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net:1234', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('file');
      equal(out.href, 'https://example.net:1234/');
      equal(out.protocol, 'https:');
    }
  });
  test('#12 protocol=file on wss://x:x@example.net:1234', (ctx: TestContext): void => {
    let u = URL.parse('wss://x:x@example.net:1234', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('file');
      equal(out.href, 'wss://x:x@example.net:1234/');
      equal(out.protocol, 'wss:');
    }
  });
  test('#13 protocol=http on file://localhost/ — Can’t switch from file URL with no host', (ctx: TestContext): void => {
    let u = URL.parse('file://localhost/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('http');
      equal(out.href, 'file:///');
      equal(out.protocol, 'file:');
    }
  });
  test('#14 protocol=https on file:///test', (ctx: TestContext): void => {
    let u = URL.parse('file:///test', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('https');
      equal(out.href, 'file:///test');
      equal(out.protocol, 'file:');
    }
  });
  test('#15 protocol=wss on file:', (ctx: TestContext): void => {
    let u = URL.parse('file:', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('wss');
      equal(out.href, 'file:///');
      equal(out.protocol, 'file:');
    }
  });
  test('#16 protocol=b on http://example.net — Can’t switch from special scheme to non-special', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('b');
      equal(out.href, 'http://example.net/');
      equal(out.protocol, 'http:');
    }
  });
  test('#17 protocol=s on file://hi/path', (ctx: TestContext): void => {
    let u = URL.parse('file://hi/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('s');
      equal(out.href, 'file://hi/path');
      equal(out.protocol, 'file:');
    }
  });
  test('#18 protocol=s on https://example.net', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('s');
      equal(out.href, 'https://example.net/');
      equal(out.protocol, 'https:');
    }
  });
  test('#19 protocol=test on ftp://example.net', (ctx: TestContext): void => {
    let u = URL.parse('ftp://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('test');
      equal(out.href, 'ftp://example.net/');
      equal(out.protocol, 'ftp:');
    }
  });
  test('#20 protocol=http on mailto:me@example.net — Cannot-be-a-base URL doesn’t have a host, but URL in a special...', (ctx: TestContext): void => {
    let u = URL.parse('mailto:me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('http');
      equal(out.href, 'mailto:me@example.net');
      equal(out.protocol, 'mailto:');
    }
  });
  test('#21 protocol=http on ssh://me@example.net — Can’t switch from non-special scheme to special', (ctx: TestContext): void => {
    let u = URL.parse('ssh://me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('http');
      equal(out.href, 'ssh://me@example.net');
      equal(out.protocol, 'ssh:');
    }
  });
  test('#22 protocol=https on ssh://me@example.net', (ctx: TestContext): void => {
    let u = URL.parse('ssh://me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('https');
      equal(out.href, 'ssh://me@example.net');
      equal(out.protocol, 'ssh:');
    }
  });
  test('#23 protocol=file on ssh://me@example.net', (ctx: TestContext): void => {
    let u = URL.parse('ssh://me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('file');
      equal(out.href, 'ssh://me@example.net');
      equal(out.protocol, 'ssh:');
    }
  });
  test('#24 protocol=file on ssh://example.net', (ctx: TestContext): void => {
    let u = URL.parse('ssh://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('file');
      equal(out.href, 'ssh://example.net');
      equal(out.protocol, 'ssh:');
    }
  });
  test('#25 protocol=https on nonsense:///test', (ctx: TestContext): void => {
    let u = URL.parse('nonsense:///test', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('https');
      equal(out.href, 'nonsense:///test');
      equal(out.protocol, 'nonsense:');
    }
  });
  test('#26 protocol=https:foo : bar on http://example.net — Stuff after the first \':\' is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('https:foo : bar');
      equal(out.href, 'https://example.net/');
      equal(out.protocol, 'https:');
    }
  });
  test('#27 protocol=view-source+data:foo : bar on data:text/html,<p>Test — Stuff after the first \':\' is ignored', (ctx: TestContext): void => {
    let u = URL.parse('data:text/html,<p>Test', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('view-source+data:foo : bar');
      equal(out.href, 'view-source+data:text/html,<p>Test');
      equal(out.protocol, 'view-source+data:');
    }
  });
  test('#28 protocol=https on http://foo.com:443/ — Port is set to null if it is the default for new scheme.', (ctx: TestContext): void => {
    let u = URL.parse('http://foo.com:443/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('https');
      equal(out.href, 'https://foo.com/');
      equal(out.protocol, 'https:');
      equal(out.port, '');
    }
  });
  test('#29 protocol=h..tt.ps on http://test/ — Tab and newline are stripped', (ctx: TestContext): void => {
    let u = URL.parse('http://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('h\r\ntt\tps');
      equal(out.href, 'https://test/');
      equal(out.protocol, 'https:');
      equal(out.port, '');
    }
  });
  test('#30 protocol=https. on http://test/', (ctx: TestContext): void => {
    let u = URL.parse('http://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('https\r');
      equal(out.href, 'https://test/');
      equal(out.protocol, 'https:');
    }
  });
  test('#31 protocol=https. on http://test/ — Non-tab/newline C0 controls result in no-op', (ctx: TestContext): void => {
    let u = URL.parse('http://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol(dec('https%00'));
      equal(out.href, 'http://test/');
      equal(out.protocol, 'http:');
    }
  });
  test('#32 protocol=https. on http://test/', (ctx: TestContext): void => {
    let u = URL.parse('http://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol(dec('https%0C'));
      equal(out.href, 'http://test/');
      equal(out.protocol, 'http:');
    }
  });
  test('#33 protocol=https. on http://test/', (ctx: TestContext): void => {
    let u = URL.parse('http://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol(dec('https%0E'));
      equal(out.href, 'http://test/');
      equal(out.protocol, 'http:');
    }
  });
  test('#34 protocol=https  on http://test/', (ctx: TestContext): void => {
    let u = URL.parse('http://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withProtocol('https ');
      equal(out.href, 'http://test/');
      equal(out.protocol, 'http:');
    }
  });
  test('#0 username=me on file:///home/you/index.html — No host means no username', (ctx: TestContext): void => {
    let u = URL.parse('file:///home/you/index.html', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('me');
      equal(out.href, 'file:///home/you/index.html');
      equal(out.username, '');
    }
  });
  test('#1 username=me on unix:/run/foo.socket — No host means no username', (ctx: TestContext): void => {
    let u = URL.parse('unix:/run/foo.socket', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('me');
      equal(out.href, 'unix:/run/foo.socket');
      equal(out.username, '');
    }
  });
  test('#2 username=me on mailto:you@example.net — Cannot-be-a-base means no username', (ctx: TestContext): void => {
    let u = URL.parse('mailto:you@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('me');
      equal(out.href, 'mailto:you@example.net');
      equal(out.username, '');
    }
  });
  test('#3 username=wario on javascript:alert(1)', (ctx: TestContext): void => {
    let u = URL.parse('javascript:alert(1)', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('wario');
      equal(out.href, 'javascript:alert(1)');
      equal(out.username, '');
    }
  });
  test('#4 username=me on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('me');
      equal(out.href, 'http://me@example.net/');
      equal(out.username, 'me');
    }
  });
  test('#5 username=me on http://:secret@example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://:secret@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('me');
      equal(out.href, 'http://me:secret@example.net/');
      equal(out.username, 'me');
    }
  });
  test('#6 username= on http://me@example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('');
      equal(out.href, 'http://example.net/');
      equal(out.username, '');
    }
  });
  test('#7 username= on http://me:secret@example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://me:secret@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('');
      equal(out.href, 'http://:secret@example.net/');
      equal(out.username, '');
    }
  });
  test('#8 username=...... !"#$%&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~.Éé on http://example.net — UTF-8 percent enco...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername(dec('%00%01%09%0A%0D%1F !"#$%25&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~%7FÉé'));
      equal(out.href, 'http://%00%01%09%0A%0D%1F%20!%22%23$%&\'()*+,-.%2F09%3A%3B%3C%3D%3E%3F%40AZ%5B%5C%5D%5E_%60az%7B%7C%7D~%7F%C2%80%C2%81%C3%89%C3%A9@example.net/');
      equal(out.username, '%00%01%09%0A%0D%1F%20!%22%23$%&\'()*+,-.%2F09%3A%3B%3C%3D%3E%3F%40AZ%5B%5C%5D%5E_%60az%7B%7C%7D~%7F%C2%80%C2%81%C3%89%C3%A9');
    }
  });
  test('#9 username=%c3%89té on http://example.net — Bytes already percent-encoded are left as-is.', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('%c3%89té');
      equal(out.href, 'http://%c3%89t%C3%A9@example.net/');
      equal(out.username, '%c3%89t%C3%A9');
    }
  });
  test('#10 username=x on sc:///', (ctx: TestContext): void => {
    let u = URL.parse('sc:///', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('x');
      equal(out.href, 'sc:///');
      equal(out.username, '');
    }
  });
  test('#11 username=wario on javascript://x/', (ctx: TestContext): void => {
    let u = URL.parse('javascript://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('wario');
      equal(out.href, 'javascript://wario@x/');
      equal(out.username, 'wario');
    }
  });
  test('#12 username=test on file://test/', (ctx: TestContext): void => {
    let u = URL.parse('file://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withUsername('test');
      equal(out.href, 'file://test/');
      equal(out.username, '');
    }
  });
  test('#0 password=secret on file:///home/me/index.html — No host means no password', (ctx: TestContext): void => {
    let u = URL.parse('file:///home/me/index.html', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('secret');
      equal(out.href, 'file:///home/me/index.html');
      equal(out.password, '');
    }
  });
  test('#1 password=secret on unix:/run/foo.socket — No host means no password', (ctx: TestContext): void => {
    let u = URL.parse('unix:/run/foo.socket', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('secret');
      equal(out.href, 'unix:/run/foo.socket');
      equal(out.password, '');
    }
  });
  test('#2 password=secret on mailto:me@example.net — Cannot-be-a-base means no password', (ctx: TestContext): void => {
    let u = URL.parse('mailto:me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('secret');
      equal(out.href, 'mailto:me@example.net');
      equal(out.password, '');
    }
  });
  test('#3 password=secret on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('secret');
      equal(out.href, 'http://:secret@example.net/');
      equal(out.password, 'secret');
    }
  });
  test('#4 password=secret on http://me@example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('secret');
      equal(out.href, 'http://me:secret@example.net/');
      equal(out.password, 'secret');
    }
  });
  test('#5 password= on http://:secret@example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://:secret@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('');
      equal(out.href, 'http://example.net/');
      equal(out.password, '');
    }
  });
  test('#6 password= on http://me:secret@example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://me:secret@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('');
      equal(out.href, 'http://me@example.net/');
      equal(out.password, '');
    }
  });
  test('#7 password=...... !"#$%&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~.Éé on http://example.net — UTF-8 percent enco...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword(dec('%00%01%09%0A%0D%1F !"#$%25&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~%7FÉé'));
      equal(out.href, 'http://:%00%01%09%0A%0D%1F%20!%22%23$%&\'()*+,-.%2F09%3A%3B%3C%3D%3E%3F%40AZ%5B%5C%5D%5E_%60az%7B%7C%7D~%7F%C2%80%C2%81%C3%89%C3%A9@example.net/');
      equal(out.password, '%00%01%09%0A%0D%1F%20!%22%23$%&\'()*+,-.%2F09%3A%3B%3C%3D%3E%3F%40AZ%5B%5C%5D%5E_%60az%7B%7C%7D~%7F%C2%80%C2%81%C3%89%C3%A9');
    }
  });
  test('#8 password=%c3%89té on http://example.net — Bytes already percent-encoded are left as-is.', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('%c3%89té');
      equal(out.href, 'http://:%c3%89t%C3%A9@example.net/');
      equal(out.password, '%c3%89t%C3%A9');
    }
  });
  test('#9 password=x on sc:///', (ctx: TestContext): void => {
    let u = URL.parse('sc:///', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('x');
      equal(out.href, 'sc:///');
      equal(out.password, '');
    }
  });
  test('#10 password=bowser on javascript://x/', (ctx: TestContext): void => {
    let u = URL.parse('javascript://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('bowser');
      equal(out.href, 'javascript://:bowser@x/');
      equal(out.password, 'bowser');
    }
  });
  test('#11 password=test on file://test/', (ctx: TestContext): void => {
    let u = URL.parse('file://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPassword('test');
      equal(out.href, 'file://test/');
      equal(out.password, '');
    }
  });
  test('#0 host=. on sc://x/ — Non-special scheme', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost(dec('%00'));
      equal(out.href, 'sc://x/');
      equal(out.host(), 'x');
      equal(out.hostname, 'x');
    }
  });
  test('#1 host=. on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('\t');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#2 host=. on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('\n');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#3 host=. on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('\r');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#4 host=  on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost(' ');
      equal(out.href, 'sc://x/');
      equal(out.host(), 'x');
      equal(out.hostname, 'x');
    }
  });
  test('#5 host=# on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('#');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#6 host=/ on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('/');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#7 host=? on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('?');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#8 host=@ on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('@');
      equal(out.href, 'sc://x/');
      equal(out.host(), 'x');
      equal(out.hostname, 'x');
    }
  });
  test('#9 host=ß on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('ß');
      equal(out.href, 'sc://%C3%9F/');
      equal(out.host(), '%C3%9F');
      equal(out.hostname, '%C3%9F');
    }
  });
  test('#10 host=ß on https://x/ — IDNA Nontransitional_Processing', (ctx: TestContext): void => {
    let u = URL.parse('https://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('ß');
      equal(out.href, 'https://xn--zca/');
      equal(out.host(), 'xn--zca');
      equal(out.hostname, 'xn--zca');
    }
  });
  test('#11 host=example.com on mailto:me@example.net — Cannot-be-a-base means no host', (ctx: TestContext): void => {
    let u = URL.parse('mailto:me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com');
      equal(out.href, 'mailto:me@example.net');
      equal(out.host(), '');
    }
  });
  test('#12 host=example.net on data:text/plain,Stuff — Cannot-be-a-base means no host', (ctx: TestContext): void => {
    let u = URL.parse('data:text/plain,Stuff', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.net');
      equal(out.href, 'data:text/plain,Stuff');
      equal(out.host(), '');
    }
  });
  test('#13 host=example.com:8080 on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080');
      equal(out.href, 'http://example.com:8080/');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#14 host=example.com on http://example.net:8080 — Port number is unchanged if not specified in the new value', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com');
      equal(out.href, 'http://example.com:8080/');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#15 host=example.com: on http://example.net:8080 — Port number is unchanged if not specified', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:');
      equal(out.href, 'http://example.com:8080/');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#16 host= on http://example.net — The empty host is not valid for special schemes', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
    }
  });
  test('#17 host= on view-source+http://example.net/foo — The empty host is OK for non-special schemes', (ctx: TestContext): void => {
    let u = URL.parse('view-source+http://example.net/foo', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('');
      equal(out.href, 'view-source+http:///foo');
      equal(out.host(), '');
    }
  });
  test('#18 host=example.net on a:/foo — Path-only URLs can gain a host', (ctx: TestContext): void => {
    let u = URL.parse('a:/foo', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.net');
      equal(out.href, 'a://example.net/foo');
      equal(out.host(), 'example.net');
    }
  });
  test('#19 host=0x7F000001:8080 on http://example.net — IPv4 address syntax is normalized', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('0x7F000001:8080');
      equal(out.href, 'http://127.0.0.1:8080/');
      equal(out.host(), '127.0.0.1:8080');
      equal(out.hostname, '127.0.0.1');
      equal(out.port, '8080');
    }
  });
  test('#20 host=[::0:01]:2 on http://example.net — IPv6 address syntax is normalized', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[::0:01]:2');
      equal(out.href, 'http://[::1]:2/');
      equal(out.host(), '[::1]:2');
      equal(out.hostname, '[::1]');
      equal(out.port, '2');
    }
  });
  test('#21 host=[2001:db8::2]:4002 on http://example.net — IPv6 literal address with port, crbug.com/1012416', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[2001:db8::2]:4002');
      equal(out.href, 'http://[2001:db8::2]:4002/');
      equal(out.host(), '[2001:db8::2]:4002');
      equal(out.hostname, '[2001:db8::2]');
      equal(out.port, '4002');
    }
  });
  test('#22 host=example.com:80 on http://example.net — Default port number is removed', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:80');
      equal(out.href, 'http://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#23 host=example.com:443 on https://example.net — Default port number is removed', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:443');
      equal(out.href, 'https://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#24 host=example.com:80 on https://example.net — Default port number is only removed for the relevant scheme', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:80');
      equal(out.href, 'https://example.com:80/');
      equal(out.host(), 'example.com:80');
      equal(out.hostname, 'example.com');
      equal(out.port, '80');
    }
  });
  test('#25 host=example.com:80 on http://example.net:8080 — Port number is removed if new port is scheme default a...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:80');
      equal(out.href, 'http://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#26 host=example.com/stuff on http://example.net/path — Stuff after a / delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com/stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#27 host=example.com:8080/stuff on http://example.net/path — Stuff after a / delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080/stuff');
      equal(out.href, 'http://example.com:8080/path');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#28 host=example.com?stuff on http://example.net/path — Stuff after a ? delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com?stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#29 host=example.com?stuff:8080 on http://example.net/path — Stuff after a ? delimiter is ignored, trailing...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com?stuff:8080');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#30 host=example.com:8080?stuff on http://example.net/path — Stuff after a ? delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080?stuff');
      equal(out.href, 'http://example.com:8080/path');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#31 host=example.com#stuff on http://example.net/path — Stuff after a # delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com#stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#32 host=example.com:8080#stuff on http://example.net/path — Stuff after a # delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080#stuff');
      equal(out.href, 'http://example.com:8080/path');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#33 host=example.com\\stuff on http://example.net/path — Stuff after a \\ delimiter is ignored for special ...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com\\stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#34 host=example.com:8080\\stuff on http://example.net/path — Stuff after a \\ delimiter is ignored for spe...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080\\stuff');
      equal(out.href, 'http://example.com:8080/path');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#35 host=example.com\\stuff on view-source+http://example.net/path — \\ is not a delimiter for non-special ...', (ctx: TestContext): void => {
    let u = URL.parse('view-source+http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com\\stuff');
      equal(out.href, 'view-source+http://example.net/path');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
      equal(out.port, '');
    }
  });
  test('#36 host=example.com:8080stuff2 on view-source+http://example.net/path — Anything other than ASCII digit st...', (ctx: TestContext): void => {
    let u = URL.parse('view-source+http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080stuff2');
      equal(out.href, 'view-source+http://example.com:8080/path');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#37 host=example.com:8080stuff2 on http://example.net/path — Anything other than ASCII digit stops the port...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080stuff2');
      equal(out.href, 'http://example.com:8080/path');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#38 host=example.com:8080+2 on http://example.net/path — Anything other than ASCII digit stops the port par...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:8080+2');
      equal(out.href, 'http://example.com:8080/path');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#39 host=example.com:invalid on http://example.net:8080 — Anything other than ASCII digit stops the port pa...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:invalid');
      equal(out.href, 'http://example.com:8080/');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#40 host=[::1]:invalid on http://example.net:8080/test — Anything other than ASCII digit stops the port par...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080/test', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[::1]:invalid');
      equal(out.href, 'http://[::1]:8080/test');
      equal(out.host(), '[::1]:8080');
      equal(out.hostname, '[::1]');
      equal(out.port, '8080');
    }
  });
  test('#41 host=[::1] on http://example.net:8080/test — IPv6 without port', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080/test', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[::1]');
      equal(out.href, 'http://[::1]:8080/test');
      equal(out.host(), '[::1]:8080');
      equal(out.hostname, '[::1]');
      equal(out.port, '8080');
    }
  });
  test('#42 host=example.com:65535 on http://example.net/path — Port numbers are 16 bit integers', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:65535');
      equal(out.href, 'http://example.com:65535/path');
      equal(out.host(), 'example.com:65535');
      equal(out.hostname, 'example.com');
      equal(out.port, '65535');
    }
  });
  test('#43 host=example.com:65536 on http://example.net/path — Port numbers are 16 bit integers, overflowing is an...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('example.com:65536');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#44 host=[google.com] on http://example.net/ — Broken IPv6', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[google.com]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#45 host=[::1.2.3.4x] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[::1.2.3.4x]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#46 host=[::1.2.3.] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[::1.2.3.]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#47 host=[::1.2.] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[::1.2.]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#48 host=[::1.] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('[::1.]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#49 host=x:123 on file://y/', (ctx: TestContext): void => {
    let u = URL.parse('file://y/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('x:123');
      equal(out.href, 'file://y/');
      equal(out.host(), 'y');
      equal(out.hostname, 'y');
      equal(out.port, '');
    }
  });
  test('#50 host=loc%41lhost on file://y/', (ctx: TestContext): void => {
    let u = URL.parse('file://y/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('loc%41lhost');
      equal(out.href, 'file:///');
      equal(out.host(), '');
      equal(out.hostname, '');
      equal(out.port, '');
    }
  });
  test('#51 host= on file://hi/x', (ctx: TestContext): void => {
    let u = URL.parse('file://hi/x', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('');
      equal(out.href, 'file:///x');
      equal(out.host(), '');
      equal(out.hostname, '');
      equal(out.port, '');
    }
  });
  test('#52 host= on sc://test@test/', (ctx: TestContext): void => {
    let u = URL.parse('sc://test@test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('');
      equal(out.href, 'sc://test@test/');
      equal(out.host(), 'test');
      equal(out.hostname, 'test');
      equal(out.username, 'test');
    }
  });
  test('#53 host= on sc://test:12/', (ctx: TestContext): void => {
    let u = URL.parse('sc://test:12/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('');
      equal(out.href, 'sc://test:12/');
      equal(out.host(), 'test:12');
      equal(out.hostname, 'test');
      equal(out.port, '12');
    }
  });
  test('#54 host=///bad.com on http://example.com/ — Leading / is not stripped', (ctx: TestContext): void => {
    let u = URL.parse('http://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('///bad.com');
      equal(out.href, 'http://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
    }
  });
  test('#55 host=///bad.com on sc://example.com/ — Leading / is not stripped', (ctx: TestContext): void => {
    let u = URL.parse('sc://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('///bad.com');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#56 host=a%C2%ADb on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('a%C2%ADb');
      equal(out.href, 'https://ab/');
      equal(out.host(), 'ab');
      equal(out.hostname, 'ab');
    }
  });
  test('#57 host=­ on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('­');
      equal(out.href, 'https://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
    }
  });
  test('#58 host=%C2%AD on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('%C2%AD');
      equal(out.href, 'https://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
    }
  });
  test('#59 host=xn-- on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('xn--');
      equal(out.href, 'https://xn--/');
      equal(out.host(), 'xn--');
      equal(out.hostname, 'xn--');
    }
  });
  test('#60 host=* on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('*');
      equal(out.href, 'https://*/');
      equal(out.host(), '*');
      equal(out.hostname, '*');
    }
  });
  test('#61 host=x@x on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('x@x');
      equal(out.href, 'https://test.invalid/');
      equal(out.host(), 'test.invalid');
      equal(out.hostname, 'test.invalid');
    }
  });
  test('#62 host=foo...bar on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('foo\t\r\nbar');
      equal(out.href, 'https://foobar/');
      equal(out.host(), 'foobar');
      equal(out.hostname, 'foobar');
    }
  });
  test('#63 host=>< on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('><');
      equal(out.href, 'https://test.invalid/');
      equal(out.host(), 'test.invalid');
      equal(out.hostname, 'test.invalid');
    }
  });
  test('#64 host=test/@aaa on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('test/@aaa');
      equal(out.href, 'https://test/');
      equal(out.host(), 'test');
      equal(out.hostname, 'test');
    }
  });
  test('#65 host=test/:aaa on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost('test/:aaa');
      equal(out.href, 'https://test/');
      equal(out.host(), 'test');
      equal(out.hostname, 'test');
    }
  });
  test('#66 host=:80 on foo://path/to', (ctx: TestContext): void => {
    let u = URL.parse('foo://path/to', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHost(':80');
      equal(out.href, 'foo://path/to');
      equal(out.host(), 'path');
      equal(out.port, '');
    }
  });
  test('#0 hostname=. on sc://x/ — Non-special scheme', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname(dec('%00'));
      equal(out.href, 'sc://x/');
      equal(out.host(), 'x');
      equal(out.hostname, 'x');
    }
  });
  test('#1 hostname=. on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('\t');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#2 hostname=. on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('\n');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#3 hostname=. on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('\r');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#4 hostname=  on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname(' ');
      equal(out.href, 'sc://x/');
      equal(out.host(), 'x');
      equal(out.hostname, 'x');
    }
  });
  test('#5 hostname=# on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('#');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#6 hostname=/ on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('/');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#7 hostname=? on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('?');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#8 hostname=@ on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('@');
      equal(out.href, 'sc://x/');
      equal(out.host(), 'x');
      equal(out.hostname, 'x');
    }
  });
  test('#9 hostname=example.com on mailto:me@example.net — Cannot-be-a-base means no host', (ctx: TestContext): void => {
    let u = URL.parse('mailto:me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com');
      equal(out.href, 'mailto:me@example.net');
      equal(out.host(), '');
    }
  });
  test('#10 hostname=example.net on data:text/plain,Stuff — Cannot-be-a-base means no host', (ctx: TestContext): void => {
    let u = URL.parse('data:text/plain,Stuff', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.net');
      equal(out.href, 'data:text/plain,Stuff');
      equal(out.host(), '');
    }
  });
  test('#11 hostname=example.com on http://example.net:8080', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com');
      equal(out.href, 'http://example.com:8080/');
      equal(out.host(), 'example.com:8080');
      equal(out.hostname, 'example.com');
      equal(out.port, '8080');
    }
  });
  test('#12 hostname= on http://example.net — The empty host is not valid for special schemes', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
    }
  });
  test('#13 hostname= on view-source+http://example.net/foo — The empty host is OK for non-special schemes', (ctx: TestContext): void => {
    let u = URL.parse('view-source+http://example.net/foo', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('');
      equal(out.href, 'view-source+http:///foo');
      equal(out.host(), '');
    }
  });
  test('#14 hostname=example.net on a:/foo — Path-only URLs can gain a host', (ctx: TestContext): void => {
    let u = URL.parse('a:/foo', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.net');
      equal(out.href, 'a://example.net/foo');
      equal(out.host(), 'example.net');
    }
  });
  test('#15 hostname=0x7F000001 on http://example.net:8080 — IPv4 address syntax is normalized', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('0x7F000001');
      equal(out.href, 'http://127.0.0.1:8080/');
      equal(out.host(), '127.0.0.1:8080');
      equal(out.hostname, '127.0.0.1');
      equal(out.port, '8080');
    }
  });
  test('#16 hostname=[::0:01] on http://example.net — IPv6 address syntax is normalized', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('[::0:01]');
      equal(out.href, 'http://[::1]/');
      equal(out.host(), '[::1]');
      equal(out.hostname, '[::1]');
      equal(out.port, '');
    }
  });
  test('#17 hostname=example.com:8080 on http://example.net/path — : delimiter invalidates entire value', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com:8080');
      equal(out.href, 'http://example.net/path');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
      equal(out.port, '');
    }
  });
  test('#18 hostname=example.com: on http://example.net:8080/path — : delimiter invalidates entire value', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com:');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#19 hostname=example.com/stuff on http://example.net/path — Stuff after a / delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com/stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#20 hostname=example.com?stuff on http://example.net/path — Stuff after a ? delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com?stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#21 hostname=example.com#stuff on http://example.net/path — Stuff after a # delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com#stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#22 hostname=example.com\\stuff on http://example.net/path — Stuff after a \\ delimiter is ignored for spec...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com\\stuff');
      equal(out.href, 'http://example.com/path');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
      equal(out.port, '');
    }
  });
  test('#23 hostname=example.com\\stuff on view-source+http://example.net/path — \\ is not a delimiter for non-spec...', (ctx: TestContext): void => {
    let u = URL.parse('view-source+http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('example.com\\stuff');
      equal(out.href, 'view-source+http://example.net/path');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
      equal(out.port, '');
    }
  });
  test('#24 hostname=[google.com] on http://example.net/ — Broken IPv6', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('[google.com]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#25 hostname=[::1.2.3.4x] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('[::1.2.3.4x]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#26 hostname=[::1.2.3.] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('[::1.2.3.]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#27 hostname=[::1.2.] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('[::1.2.]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#28 hostname=[::1.] on http://example.net/', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('[::1.]');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
    }
  });
  test('#29 hostname=x:123 on file://y/', (ctx: TestContext): void => {
    let u = URL.parse('file://y/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('x:123');
      equal(out.href, 'file://y/');
      equal(out.host(), 'y');
      equal(out.hostname, 'y');
      equal(out.port, '');
    }
  });
  test('#30 hostname=loc%41lhost on file://y/', (ctx: TestContext): void => {
    let u = URL.parse('file://y/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('loc%41lhost');
      equal(out.href, 'file:///');
      equal(out.host(), '');
      equal(out.hostname, '');
      equal(out.port, '');
    }
  });
  test('#31 hostname= on file://hi/x', (ctx: TestContext): void => {
    let u = URL.parse('file://hi/x', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('');
      equal(out.href, 'file:///x');
      equal(out.host(), '');
      equal(out.hostname, '');
      equal(out.port, '');
    }
  });
  test('#32 hostname= on sc://test@test/', (ctx: TestContext): void => {
    let u = URL.parse('sc://test@test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('');
      equal(out.href, 'sc://test@test/');
      equal(out.host(), 'test');
      equal(out.hostname, 'test');
      equal(out.username, 'test');
    }
  });
  test('#33 hostname= on sc://test:12/', (ctx: TestContext): void => {
    let u = URL.parse('sc://test:12/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('');
      equal(out.href, 'sc://test:12/');
      equal(out.host(), 'test:12');
      equal(out.hostname, 'test');
      equal(out.port, '12');
    }
  });
  test('#34 hostname=h on non-spec:/.//p — Drop /. from path', (ctx: TestContext): void => {
    let u = URL.parse('non-spec:/.//p', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('h');
      equal(out.href, 'non-spec://h//p');
      equal(out.host(), 'h');
      equal(out.hostname, 'h');
      equal(out.pathname, '//p');
    }
  });
  test('#35 hostname= on non-spec:/.//p', (ctx: TestContext): void => {
    let u = URL.parse('non-spec:/.//p', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('');
      equal(out.href, 'non-spec:////p');
      equal(out.host(), '');
      equal(out.hostname, '');
      equal(out.pathname, '//p');
    }
  });
  test('#36 hostname=///bad.com on http://example.com/ — Leading / is not stripped', (ctx: TestContext): void => {
    let u = URL.parse('http://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('///bad.com');
      equal(out.href, 'http://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
    }
  });
  test('#37 hostname=///bad.com on sc://example.com/ — Leading / is not stripped', (ctx: TestContext): void => {
    let u = URL.parse('sc://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('///bad.com');
      equal(out.href, 'sc:///');
      equal(out.host(), '');
      equal(out.hostname, '');
    }
  });
  test('#38 hostname=a%C2%ADb on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('a%C2%ADb');
      equal(out.href, 'https://ab/');
      equal(out.host(), 'ab');
      equal(out.hostname, 'ab');
    }
  });
  test('#39 hostname=­ on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('­');
      equal(out.href, 'https://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
    }
  });
  test('#40 hostname=%C2%AD on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('%C2%AD');
      equal(out.href, 'https://example.com/');
      equal(out.host(), 'example.com');
      equal(out.hostname, 'example.com');
    }
  });
  test('#41 hostname=xn-- on https://example.com/', (ctx: TestContext): void => {
    let u = URL.parse('https://example.com/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('xn--');
      equal(out.href, 'https://xn--/');
      equal(out.host(), 'xn--');
      equal(out.hostname, 'xn--');
    }
  });
  test('#42 hostname=* on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('*');
      equal(out.href, 'https://*/');
      equal(out.host(), '*');
      equal(out.hostname, '*');
    }
  });
  test('#43 hostname=x@x on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('x@x');
      equal(out.href, 'https://test.invalid/');
      equal(out.host(), 'test.invalid');
      equal(out.hostname, 'test.invalid');
    }
  });
  test('#44 hostname=foo...bar on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('foo\t\r\nbar');
      equal(out.href, 'https://foobar/');
      equal(out.host(), 'foobar');
      equal(out.hostname, 'foobar');
    }
  });
  test('#45 hostname=>< on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('><');
      equal(out.href, 'https://test.invalid/');
      equal(out.host(), 'test.invalid');
      equal(out.hostname, 'test.invalid');
    }
  });
  test('#46 hostname=test/@aaa on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('test/@aaa');
      equal(out.href, 'https://test/');
      equal(out.host(), 'test');
      equal(out.hostname, 'test');
    }
  });
  test('#47 hostname=test/:aaa on https://test.invalid/', (ctx: TestContext): void => {
    let u = URL.parse('https://test.invalid/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHostname('test/:aaa');
      equal(out.href, 'https://test/');
      equal(out.host(), 'test');
      equal(out.hostname, 'test');
    }
  });
  test('#0 port=8080 on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080');
      equal(out.href, 'http://example.net:8080/');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#1 port= on http://example.net:8080 — Port number is removed if empty is the new value', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
      equal(out.port, '');
    }
  });
  test('#2 port=80 on http://example.net:8080 — Default port number is removed', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('80');
      equal(out.href, 'http://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
      equal(out.port, '');
    }
  });
  test('#3 port=443 on https://example.net:4433 — Default port number is removed', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net:4433', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('443');
      equal(out.href, 'https://example.net/');
      equal(out.host(), 'example.net');
      equal(out.hostname, 'example.net');
      equal(out.port, '');
    }
  });
  test('#4 port=80 on https://example.net — Default port number is only removed for the relevant scheme', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('80');
      equal(out.href, 'https://example.net:80/');
      equal(out.host(), 'example.net:80');
      equal(out.hostname, 'example.net');
      equal(out.port, '80');
    }
  });
  test('#5 port=8080/stuff on http://example.net/path — Stuff after a / delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080/stuff');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#6 port=8080?stuff on http://example.net/path — Stuff after a ? delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080?stuff');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#7 port=8080#stuff on http://example.net/path — Stuff after a # delimiter is ignored', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080#stuff');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#8 port=8080\\stuff on http://example.net/path — Stuff after a \\ delimiter is ignored for special schemes', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080\\stuff');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#9 port=8080stuff2 on view-source+http://example.net/path — Anything other than ASCII digit stops the port ...', (ctx: TestContext): void => {
    let u = URL.parse('view-source+http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080stuff2');
      equal(out.href, 'view-source+http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#10 port=8080stuff2 on http://example.net/path — Anything other than ASCII digit stops the port parser in a...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080stuff2');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#11 port=8080+2 on http://example.net/path — Anything other than ASCII digit stops the port parser in a set...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('8080+2');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#12 port=65535 on http://example.net/path — Port numbers are 16 bit integers', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('65535');
      equal(out.href, 'http://example.net:65535/path');
      equal(out.host(), 'example.net:65535');
      equal(out.hostname, 'example.net');
      equal(out.port, '65535');
    }
  });
  test('#13 port=65536 on http://example.net:8080/path — Port numbers are 16 bit integers, overflowing is an error', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('65536');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#14 port=randomstring on http://example.net:8080/path — Setting port to a string that doesn\'t parse as a n...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net:8080/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('randomstring');
      equal(out.href, 'http://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#15 port=65536 on non-special://example.net:8080/path — Port numbers are 16 bit integers, overflowing is an...', (ctx: TestContext): void => {
    let u = URL.parse('non-special://example.net:8080/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('65536');
      equal(out.href, 'non-special://example.net:8080/path');
      equal(out.host(), 'example.net:8080');
      equal(out.hostname, 'example.net');
      equal(out.port, '8080');
    }
  });
  test('#16 port=12 on file://test/', (ctx: TestContext): void => {
    let u = URL.parse('file://test/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('12');
      equal(out.href, 'file://test/');
      equal(out.port, '');
    }
  });
  test('#17 port=12 on file://localhost/', (ctx: TestContext): void => {
    let u = URL.parse('file://localhost/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('12');
      equal(out.href, 'file:///');
      equal(out.port, '');
    }
  });
  test('#18 port=12 on non-base:value', (ctx: TestContext): void => {
    let u = URL.parse('non-base:value', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('12');
      equal(out.href, 'non-base:value');
      equal(out.port, '');
    }
  });
  test('#19 port=12 on sc:///', (ctx: TestContext): void => {
    let u = URL.parse('sc:///', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('12');
      equal(out.href, 'sc:///');
      equal(out.port, '');
    }
  });
  test('#20 port=12 on sc://x/', (ctx: TestContext): void => {
    let u = URL.parse('sc://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('12');
      equal(out.href, 'sc://x:12/');
      equal(out.port, '12');
    }
  });
  test('#21 port=12 on javascript://x/', (ctx: TestContext): void => {
    let u = URL.parse('javascript://x/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('12');
      equal(out.href, 'javascript://x:12/');
      equal(out.port, '12');
    }
  });
  test('#22 port=.8080 on https://domain.com:443 — Leading u0009 on special scheme', (ctx: TestContext): void => {
    let u = URL.parse('https://domain.com:443', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('\t8080');
      equal(out.href, 'https://domain.com:8080/');
      equal(out.port, '8080');
    }
  });
  test('#23 port=.8080 on wpt++://domain.com:443 — Leading u0009 on non-special scheme', (ctx: TestContext): void => {
    let u = URL.parse('wpt++://domain.com:443', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('\t8080');
      equal(out.href, 'wpt++://domain.com:8080');
      equal(out.port, '8080');
    }
  });
  test('#24 port=4wpt on https://www.google.com:4343 — Should use all ascii prefixed characters as port', (ctx: TestContext): void => {
    let u = URL.parse('https://www.google.com:4343', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('4wpt');
      equal(out.href, 'https://www.google.com:4/');
      equal(out.port, '4');
    }
  });
  test('#25 port=..80..80.. on https://domain.com:3000', (ctx: TestContext): void => {
    let u = URL.parse('https://domain.com:3000', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('\n\t80\n\t80\n\t');
      equal(out.href, 'https://domain.com:8080/');
      equal(out.port, '8080');
    }
  });
  test('#26 port=.... on https://domain.com:3000', (ctx: TestContext): void => {
    let u = URL.parse('https://domain.com:3000', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPort('\n\n\t\t');
      equal(out.href, 'https://domain.com:3000/');
      equal(out.port, '3000');
    }
  });
  test('#0 pathname=/foo on mailto:me@example.net — Opaque paths cannot be set', (ctx: TestContext): void => {
    let u = URL.parse('mailto:me@example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('/foo');
      equal(out.href, 'mailto:me@example.net');
      equal(out.pathname, 'me@example.net');
    }
  });
  test('#1 pathname=new value on data:original', (ctx: TestContext): void => {
    let u = URL.parse('data:original', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('new value');
      equal(out.href, 'data:original');
      equal(out.pathname, 'original');
    }
  });
  test('#2 pathname=new value on sc:original', (ctx: TestContext): void => {
    let u = URL.parse('sc:original', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('new value');
      equal(out.href, 'sc:original');
      equal(out.pathname, 'original');
    }
  });
  test('#3 pathname= on file:///some/path — Special URLs cannot have their paths erased', (ctx: TestContext): void => {
    let u = URL.parse('file:///some/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('');
      equal(out.href, 'file:///');
      equal(out.pathname, '/');
    }
  });
  test('#4 pathname= on foo://somehost/some/path — Non-special URLs can have their paths erased', (ctx: TestContext): void => {
    let u = URL.parse('foo://somehost/some/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('');
      equal(out.href, 'foo://somehost');
      equal(out.pathname, '');
    }
  });
  test('#5 pathname= on foo:///some/path — Non-special URLs with an empty host can have their paths erased', (ctx: TestContext): void => {
    let u = URL.parse('foo:///some/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('');
      equal(out.href, 'foo://');
      equal(out.pathname, '');
    }
  });
  test('#6 pathname= on foo:/some/path — Path-only URLs cannot have their paths erased', (ctx: TestContext): void => {
    let u = URL.parse('foo:/some/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('');
      equal(out.href, 'foo:/');
      equal(out.pathname, '/');
    }
  });
  test('#7 pathname=test on foo:/some/path — Path-only URLs always have an initial slash', (ctx: TestContext): void => {
    let u = URL.parse('foo:/some/path', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('test');
      equal(out.href, 'foo:/test');
      equal(out.pathname, '/test');
    }
  });
  test('#8 pathname=/var/log/../run/bar.socket on unix:/run/foo.socket?timeout=10', (ctx: TestContext): void => {
    let u = URL.parse('unix:/run/foo.socket?timeout=10', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('/var/log/../run/bar.socket');
      equal(out.href, 'unix:/var/run/bar.socket?timeout=10');
      equal(out.pathname, '/var/run/bar.socket');
    }
  });
  test('#9 pathname=home on https://example.net#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('home');
      equal(out.href, 'https://example.net/home#nav');
      equal(out.pathname, '/home');
    }
  });
  test('#10 pathname=../home on https://example.net#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('../home');
      equal(out.href, 'https://example.net/home#nav');
      equal(out.pathname, '/home');
    }
  });
  test('#11 pathname=\\a\\%2E\\b\\%2e.\\c on http://example.net/home?lang=fr#nav — \\ is a segment delimiter for \'...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net/home?lang=fr#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('\\a\\%2E\\b\\%2e.\\c');
      equal(out.href, 'http://example.net/a/c?lang=fr#nav');
      equal(out.pathname, '/a/c');
    }
  });
  test('#12 pathname=\\a\\%2E\\b\\%2e.\\c on view-source+http://example.net/home?lang=fr#nav — \\ is *not* a segmen...', (ctx: TestContext): void => {
    let u = URL.parse('view-source+http://example.net/home?lang=fr#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('\\a\\%2E\\b\\%2e.\\c');
      equal(out.href, 'view-source+http://example.net/\\a\\%2E\\b\\%2e.\\c?lang=fr#nav');
      equal(out.pathname, '/\\a\\%2E\\b\\%2e.\\c');
    }
  });
  test('#13 pathname=...... !"#$%&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~.Éé on a:/ — UTF-8 percent encoding with the ...', (ctx: TestContext): void => {
    let u = URL.parse('a:/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname(dec('%00%01%09%0A%0D%1F !"#$%25&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~%7FÉé'));
      equal(out.href, 'a:/%00%01%1F%20!%22%23$%&\'()*+,-./09:;%3C=%3E%3F@AZ[\\]%5E_%60az%7B|%7D~%7F%C2%80%C2%81%C3%89%C3%A9');
      equal(out.pathname, '/%00%01%1F%20!%22%23$%&\'()*+,-./09:;%3C=%3E%3F@AZ[\\]%5E_%60az%7B|%7D~%7F%C2%80%C2%81%C3%89%C3%A9');
    }
  });
  test('#14 pathname=%2e%2E%c3%89té on http://example.net — Bytes already percent-encoded are left as-is, including...', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('%2e%2E%c3%89té');
      equal(out.href, 'http://example.net/%2e%2E%c3%89t%C3%A9');
      equal(out.pathname, '/%2e%2E%c3%89t%C3%A9');
    }
  });
  test('#15 pathname=? on http://example.net — ? needs to be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('?');
      equal(out.href, 'http://example.net/%3F');
      equal(out.pathname, '/%3F');
    }
  });
  test('#16 pathname=# on http://example.net — # needs to be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('#');
      equal(out.href, 'http://example.net/%23');
      equal(out.pathname, '/%23');
    }
  });
  test('#17 pathname=? on sc://example.net — ? needs to be encoded, non-special scheme', (ctx: TestContext): void => {
    let u = URL.parse('sc://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('?');
      equal(out.href, 'sc://example.net/%3F');
      equal(out.pathname, '/%3F');
    }
  });
  test('#18 pathname=# on sc://example.net — # needs to be encoded, non-special scheme', (ctx: TestContext): void => {
    let u = URL.parse('sc://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('#');
      equal(out.href, 'sc://example.net/%23');
      equal(out.pathname, '/%23');
    }
  });
  test('#19 pathname=/?é on http://example.net — ? doesn\'t mess up encoding', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('/?é');
      equal(out.href, 'http://example.net/%3F%C3%A9');
      equal(out.pathname, '/%3F%C3%A9');
    }
  });
  test('#20 pathname=/#é on http://example.net — # doesn\'t mess up encoding', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('/#é');
      equal(out.href, 'http://example.net/%23%C3%A9');
      equal(out.pathname, '/%23%C3%A9');
    }
  });
  test('#21 pathname=\\\\ on file://monkey/ — File URLs and (back)slashes', (ctx: TestContext): void => {
    let u = URL.parse('file://monkey/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('\\\\');
      equal(out.href, 'file://monkey//');
      equal(out.pathname, '//');
    }
  });
  test('#22 pathname=//\\/ on file:///unicorn — File URLs and (back)slashes', (ctx: TestContext): void => {
    let u = URL.parse('file:///unicorn', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('//\\/');
      equal(out.href, 'file://////');
      equal(out.pathname, '////');
    }
  });
  test('#23 pathname=//monkey/..// on file:///unicorn — File URLs and (back)slashes', (ctx: TestContext): void => {
    let u = URL.parse('file:///unicorn', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('//monkey/..//');
      equal(out.href, 'file://///');
      equal(out.pathname, '///');
    }
  });
  test('#24 pathname=/.//p on non-spec:/ — Serialize /. in path', (ctx: TestContext): void => {
    let u = URL.parse('non-spec:/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('/.//p');
      equal(out.href, 'non-spec:/.//p');
      equal(out.pathname, '//p');
    }
  });
  test('#25 pathname=/..//p on non-spec:/', (ctx: TestContext): void => {
    let u = URL.parse('non-spec:/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('/..//p');
      equal(out.href, 'non-spec:/.//p');
      equal(out.pathname, '//p');
    }
  });
  test('#26 pathname=//p on non-spec:/', (ctx: TestContext): void => {
    let u = URL.parse('non-spec:/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('//p');
      equal(out.href, 'non-spec:/.//p');
      equal(out.pathname, '//p');
    }
  });
  test('#27 pathname=p on non-spec:/.// — Drop /. from path', (ctx: TestContext): void => {
    let u = URL.parse('non-spec:/.//', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('p');
      equal(out.href, 'non-spec:/p');
      equal(out.pathname, '/p');
    }
  });
  test('#28 pathname=space  on data:/nospace — Non-special URLs with non-opaque paths percent-encode U+0020', (ctx: TestContext): void => {
    let u = URL.parse('data:/nospace', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('space ');
      equal(out.href, 'data:/space%20');
      equal(out.pathname, '/space%20');
    }
  });
  test('#29 pathname=space  on sc:/nospace', (ctx: TestContext): void => {
    let u = URL.parse('sc:/nospace', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('space ');
      equal(out.href, 'sc:/space%20');
      equal(out.pathname, '/space%20');
    }
  });
  test('#30 pathname=  on http://example.net — Trailing space should be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname(' ');
      equal(out.href, 'http://example.net/%20');
      equal(out.pathname, '/%20');
    }
  });
  test('#31 pathname=. on http://example.net — Trailing C0 control should be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname(dec('%00'));
      equal(out.href, 'http://example.net/%00');
      equal(out.pathname, '/%00');
    }
  });
  test('#32 pathname=/.. on foo://path/to', (ctx: TestContext): void => {
    let u = URL.parse('foo://path/to', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withPathname('/..');
      equal(out.href, 'foo://path/');
      equal(out.pathname, '/');
    }
  });
  test('#0 search=lang=fr on https://example.net#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('lang=fr');
      equal(out.href, 'https://example.net/?lang=fr#nav');
      equal(out.search, '?lang=fr');
    }
  });
  test('#1 search=lang=fr on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('lang=fr');
      equal(out.href, 'https://example.net/?lang=fr#nav');
      equal(out.search, '?lang=fr');
    }
  });
  test('#2 search=?lang=fr on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('?lang=fr');
      equal(out.href, 'https://example.net/?lang=fr#nav');
      equal(out.search, '?lang=fr');
    }
  });
  test('#3 search=??lang=fr on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('??lang=fr');
      equal(out.href, 'https://example.net/??lang=fr#nav');
      equal(out.search, '??lang=fr');
    }
  });
  test('#4 search=? on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('?');
      equal(out.href, 'https://example.net/?#nav');
      equal(out.search, '');
    }
  });
  test('#5 search= on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('');
      equal(out.href, 'https://example.net/#nav');
      equal(out.search, '');
    }
  });
  test('#6 search= on https://example.net?lang=en-US', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('');
      equal(out.href, 'https://example.net/');
      equal(out.search, '');
    }
  });
  test('#7 search= on https://example.net', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('');
      equal(out.href, 'https://example.net/');
      equal(out.search, '');
    }
  });
  test('#8 search=...... !"#$%&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~.Éé on a:/ — UTF-8 percent encoding with the que...', (ctx: TestContext): void => {
    let u = URL.parse('a:/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch(dec('%00%01%09%0A%0D%1F !"#$%25&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~%7FÉé'));
      equal(out.href, 'a:/?%00%01%1F%20!%22%23$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_`az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
      equal(out.search, '?%00%01%1F%20!%22%23$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_`az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
    }
  });
  test('#9 search=%c3%89té on http://example.net — Bytes already percent-encoded are left as-is', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('%c3%89té');
      equal(out.href, 'http://example.net/?%c3%89t%C3%A9');
      equal(out.search, '?%c3%89t%C3%A9');
    }
  });
  test('#10 search= on data:space ?query — Trailing spaces and opaque paths', (ctx: TestContext): void => {
    let u = URL.parse('data:space ?query', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('');
      equal(out.href, 'data:space%20');
      equal(out.pathname, 'space%20');
      equal(out.search, '');
    }
  });
  test('#11 search= on sc:space ?query', (ctx: TestContext): void => {
    let u = URL.parse('sc:space ?query', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('');
      equal(out.href, 'sc:space%20');
      equal(out.pathname, 'space%20');
      equal(out.search, '');
    }
  });
  test('#12 search= on data:space  ?query#fragment — Trailing spaces and opaque paths', (ctx: TestContext): void => {
    let u = URL.parse('data:space  ?query#fragment', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('');
      equal(out.href, 'data:space %20#fragment');
      equal(out.search, '');
    }
  });
  test('#13 search= on sc:space  ?query#fragment', (ctx: TestContext): void => {
    let u = URL.parse('sc:space  ?query#fragment', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch('');
      equal(out.href, 'sc:space %20#fragment');
      equal(out.search, '');
    }
  });
  test('#14 search=  on http://example.net — Trailing space should be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch(' ');
      equal(out.href, 'http://example.net/?%20');
      equal(out.search, '?%20');
    }
  });
  test('#15 search=. on http://example.net — Trailing C0 control should be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withSearch(dec('%00'));
      equal(out.href, 'http://example.net/?%00');
      equal(out.search, '?%00');
    }
  });
  test('#0 hash=main on https://example.net', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('main');
      equal(out.href, 'https://example.net/#main');
      equal(out.hash, '#main');
    }
  });
  test('#1 hash=main on https://example.net#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('main');
      equal(out.href, 'https://example.net/#main');
      equal(out.hash, '#main');
    }
  });
  test('#2 hash=##nav on https://example.net?lang=en-US', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('##nav');
      equal(out.href, 'https://example.net/?lang=en-US##nav');
      equal(out.hash, '##nav');
    }
  });
  test('#3 hash=#main on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#main');
      equal(out.href, 'https://example.net/?lang=en-US#main');
      equal(out.hash, '#main');
    }
  });
  test('#4 hash=# on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#');
      equal(out.href, 'https://example.net/?lang=en-US#');
      equal(out.hash, '');
    }
  });
  test('#5 hash= on https://example.net?lang=en-US#nav', (ctx: TestContext): void => {
    let u = URL.parse('https://example.net?lang=en-US#nav', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('');
      equal(out.href, 'https://example.net/?lang=en-US');
      equal(out.hash, '');
    }
  });
  test('#6 hash=#foo bar on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#foo bar');
      equal(out.href, 'http://example.net/#foo%20bar');
      equal(out.hash, '#foo%20bar');
    }
  });
  test('#7 hash=#foo"bar on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#foo"bar');
      equal(out.href, 'http://example.net/#foo%22bar');
      equal(out.hash, '#foo%22bar');
    }
  });
  test('#8 hash=#foo<bar on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#foo<bar');
      equal(out.href, 'http://example.net/#foo%3Cbar');
      equal(out.hash, '#foo%3Cbar');
    }
  });
  test('#9 hash=#foo>bar on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#foo>bar');
      equal(out.href, 'http://example.net/#foo%3Ebar');
      equal(out.hash, '#foo%3Ebar');
    }
  });
  test('#10 hash=#foo`bar on http://example.net', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#foo`bar');
      equal(out.href, 'http://example.net/#foo%60bar');
      equal(out.hash, '#foo%60bar');
    }
  });
  test('#11 hash=...... !"#$%&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~.Éé on a:/ — Simple percent-encoding; tabs and ne...', (ctx: TestContext): void => {
    let u = URL.parse('a:/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash(dec('%00%01%09%0A%0D%1F !"#$%25&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~%7FÉé'));
      equal(out.href, 'a:/#%00%01%1F%20!%22#$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_%60az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
      equal(out.hash, '#%00%01%1F%20!%22#$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_%60az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
    }
  });
  test('#12 hash=a.b on http://example.net — Percent-encode NULLs in fragment', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash(dec('a%00b'));
      equal(out.href, 'http://example.net/#a%00b');
      equal(out.hash, '#a%00b');
    }
  });
  test('#13 hash=a.b on non-spec:/ — Percent-encode NULLs in fragment', (ctx: TestContext): void => {
    let u = URL.parse('non-spec:/', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash(dec('a%00b'));
      equal(out.href, 'non-spec:/#a%00b');
      equal(out.hash, '#a%00b');
    }
  });
  test('#14 hash=%c3%89té on http://example.net — Bytes already percent-encoded are left as-is', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('%c3%89té');
      equal(out.href, 'http://example.net/#%c3%89t%C3%A9');
      equal(out.hash, '#%c3%89t%C3%A9');
    }
  });
  test('#15 hash=castle on javascript:alert(1)', (ctx: TestContext): void => {
    let u = URL.parse('javascript:alert(1)', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('castle');
      equal(out.href, 'javascript:alert(1)#castle');
      equal(out.hash, '#castle');
    }
  });
  test('#16 hash= on data:space                                                                                    ...', (ctx: TestContext): void => {
    let u = URL.parse('data:space                                                                                                                                  #fragment', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('');
      equal(out.href, 'data:space                                                                                                                                 %20');
      equal(out.pathname, 'space                                                                                                                                 %20');
      equal(out.hash, '');
    }
  });
  test('#17 hash= on sc:space    #fragment', (ctx: TestContext): void => {
    let u = URL.parse('sc:space    #fragment', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('');
      equal(out.href, 'sc:space   %20');
      equal(out.pathname, 'space   %20');
      equal(out.hash, '');
    }
  });
  test('#18 hash= on data:space  ?query#fragment — Trailing spaces and opaque paths', (ctx: TestContext): void => {
    let u = URL.parse('data:space  ?query#fragment', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('');
      equal(out.href, 'data:space %20?query');
      equal(out.hash, '');
    }
  });
  test('#19 hash= on sc:space  ?query#fragment', (ctx: TestContext): void => {
    let u = URL.parse('sc:space  ?query#fragment', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('');
      equal(out.href, 'sc:space %20?query');
      equal(out.hash, '');
    }
  });
  test('#20 hash=  on http://example.net — Trailing space should be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash(' ');
      equal(out.href, 'http://example.net/#%20');
      equal(out.hash, '#%20');
    }
  });
  test('#21 hash=. on http://example.net — Trailing C0 control should be encoded', (ctx: TestContext): void => {
    let u = URL.parse('http://example.net', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash(dec('%00'));
      equal(out.href, 'http://example.net/#%00');
      equal(out.hash, '#%00');
    }
  });
  test('#22 hash=#<foo> <bar> on data:text/plain,test — Fragment with <> on data: URI', (ctx: TestContext): void => {
    let u = URL.parse('data:text/plain,test', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#<foo> <bar>');
      equal(out.href, 'data:text/plain,test#%3Cfoo%3E%20%3Cbar%3E');
      equal(out.hash, '#%3Cfoo%3E%20%3Cbar%3E');
    }
  });
  test('#23 hash=#<foo> <bar> on about:blank — Fragment with <> on about:blank', (ctx: TestContext): void => {
    let u = URL.parse('about:blank', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash('#<foo> <bar>');
      equal(out.href, 'about:blank#%3Cfoo%3E%20%3Cbar%3E');
      equal(out.hash, '#%3Cfoo%3E%20%3Cbar%3E');
    }
  });
  test('#24 hash=#...... !"#$%&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~.Éé on data:text/plain,test — Fragment percent-e...', (ctx: TestContext): void => {
    let u = URL.parse('data:text/plain,test', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash(dec('#%00%01%09%0A%0D%1F !"#$%25&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~%7FÉé'));
      equal(out.href, 'data:text/plain,test#%00%01%1F%20!%22#$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_%60az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
      equal(out.hash, '#%00%01%1F%20!%22#$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_%60az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
    }
  });
  test('#25 hash=#...... !"#$%&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~.Éé on about:blank — Fragment percent-encode set...', (ctx: TestContext): void => {
    let u = URL.parse('about:blank', null);
    isTrue(u != null);
    if (u != null) {
      let out = (u as URL).withHash(dec('#%00%01%09%0A%0D%1F !"#$%25&\'()*+,-./09:;<=>?@AZ[\\]^_`az{|}~%7FÉé'));
      equal(out.href, 'about:blank#%00%01%1F%20!%22#$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_%60az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
      equal(out.hash, '#%00%01%1F%20!%22#$%&\'()*+,-./09:;%3C=%3E?@AZ[\\]^_%60az{|}~%7F%C2%80%C2%81%C3%89%C3%A9');
    }
  });
});
