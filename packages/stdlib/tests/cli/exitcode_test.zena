// Test enum with imports from cli
//
// @requires: wasmtime

import {console} from 'zena:console-wasi';
import {ExitCode} from 'zena:cli';

export let main = (): i32 => {
  console.log("Testing ExitCode enum...");
  
  let code = ExitCode.Success;
  if (code == ExitCode.Success) {
    console.log("Success works");
  }
  
  return 0;
};
