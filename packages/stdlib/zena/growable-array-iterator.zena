/**
 * Moved to core/growable-array-iterator.zena. This file exists only so the checked-in
 * bootstrap, whose baked manifest still names the old path, can resolve
 * `zena:growable-array-iterator` while building the compiler that carries the new one. The
 * next reseed removes the need for it; see
 * docs/design/stdlib-organization.md, Phase 1.
 */
export * from './core/growable-array-iterator.zena';
