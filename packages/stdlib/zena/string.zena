/**
 * Moved to core/string.zena. This file exists only so the checked-in
 * bootstrap, whose baked manifest still names the old path, can resolve
 * `zena:string` while building the compiler that carries the new one. The
 * next reseed removes the need for it; see
 * docs/design/stdlib-organization.md, Phase 1.
 */
export * from './core/string.zena';
