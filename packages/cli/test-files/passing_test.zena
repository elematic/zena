// Test file with all passing tests
import { suite, test, TestContext } from 'zena:test';
import { equal, isTrue } from 'zena:assert';

export let tests = suite('Passing Suite', (): void => {
  test('math works', (ctx: TestContext): void => {
    equal(1 + 1, 2);
  });
  
  test('booleans work', (ctx: TestContext): void => {
    isTrue(true);
  });
});
