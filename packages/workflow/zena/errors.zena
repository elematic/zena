/**
 * Raised for invalid workflow model data: duplicate step keys or input
 * names, references to unknown steps, and dependency cycles.
 */
export class WorkflowError extends Error {
  new(message: String) : super(message);
}
