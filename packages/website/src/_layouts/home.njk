---
layout: base.njk
---

{#
  The home page is a one-off, so its content lives in the page itself rather
  than in front matter — see src/index.md. This layout only supplies the
  wrappers the stylesheet needs.
#}
<zena-code-copy class="site-content is-home" id="site-content">
  <div class="home-page">{{ content | safe }}</div>
</zena-code-copy>
